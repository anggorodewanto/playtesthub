# playtesthub — Build Status

Tracks playtesthub build status. PRD version history lives in [`CHANGELOG.md`](CHANGELOG.md); full requirements live in [`PRD.md`](PRD.md); schema definitions live in [`schema.md`](schema.md); build conventions (repo layout, test strategy, TDD workflow, CI gates) live in [`engineering.md`](engineering.md).

## Status legend

- `not started` — no code written yet
- `in progress` — some code landed; milestone not yet complete
- `shipped` — milestone deliverables merged and demoed

## Milestones

Milestone scope is defined in PRD §10.

| Milestone | Scope (summary) | Status |
| --------- | --------------- | ------ |
| **M1 — Backend + admin CRUD + signup (STEAM_KEYS only)** | Postgres schema (Playtest incl. `distributionModel`/`agsItemId`/`agsCampaignId`/`initialCodeQuantity`, Applicant, Code, `leader_lease`). Admin Portal extension site with Playtest list + create/edit (+ soft-delete). Player Svelte app: landing + Discord-through-IAM login + signup form with `config.json` loader. `distributionModel` field exists but only `STEAM_KEYS` is functional. Golden flow stops at "applicant is PENDING". | shipped — phase 12.1 (axe-playwright a11y gate) deferred as M3 polish |
| **M2 — NDA + approval + code grant (pool-only) + AGS Campaign API integration** | NDA click-accept + versioned acceptance storage. Admin applicants page with approve/reject. Key pool management: STEAM_KEYS CSV upload; AGS_CAMPAIGN pool status + "Generate more codes" + "Sync from AGS". AGS Campaign API integration (Item + Campaign creation + code generation/fetch). Approve API: reserve → fenced finalize (pool-only grant, §4.1 step 6). Background reclaim job with TTL. `AuditLog` write path wired for NDA edits, playtest edits, `code.grant_orphaned`, `code.upload_rejected`, and `campaign.*` events. Golden flow stops at "player sees code in UI". | shipped — all 14 phases shipped through 2026-05-04 (proto → migration → repo → AcceptNDA → UploadCodes/GetCodePool → Approve/Reject + reclaim → DM queue + RetryDM → AGS auto-provision → TopUp/SyncFromAGS → admin pages → player views → `pth` M2 subcommands + `flow golden-m2` → e2e `TestGoldenM2Flow` → README walkthrough). SDK-backed AGS adapter (phase 8.1) still deferred — bootapp wires `ags.NewMemClient` until a live ISC namespace is available. |
| **M3 — Survey (when configured) + Discord notifications + polish/docs/demo** | Survey builder (text / rating / multi-choice, optional per playtest). Survey response submission (one-shot immutable) + responses viewer (aggregates split by `surveyId`). Discord DM notification on approval. Audit log viewer UI (paginated, filterable, JSON diff). Perf proof point: 500 signups / 10 min at p95 < 3s end-to-end. README walkthrough (including RBAC warning), demo video, public MIT repo. | not started |

## Intra-milestone ordering

Per-milestone execution sequence. Each phase is red–green TDD per [`engineering.md`](engineering.md) §4 — tests land with the code, not after. The ordering minimises rework: proto and schema lock the contracts before any handler depends on them; repository tests exercise real SQL before service handlers mock repos; per-RPC vertical slices give a demoable golden flow as early as possible.

### M1 — Backend + admin CRUD + signup (STEAM_KEYS only)

Target: golden flow stops at "applicant is PENDING".

Phase status legend: `[ ]` not started · `[~]` in progress · `[x]` shipped.

**Smoke harness policy** (cross-cutting): `scripts/smoke/` carries bash smoke scripts that exercise the real binary. `boot.sh` landed alongside phase 3 and is the minimum bar today (service boots, reflection lists all 10 M1 methods, OpenAPI spec served, an unauth RPC reaches the handler). Every phase that adds user-visible behavior extends the harness with a script that hits the RPC paths it introduces — phases 6, 7, 8, 9 all carry this sub-deliverable. The harness is deliberately bash + `grpcurl` + `curl` so it runs before the `pth` CLI exists (phase 10). Once `pth flow golden-m1` lands in phase 10, it supersedes the bash harness and becomes the authoritative e2e gate; `scripts/smoke/` can be deleted then (or kept as a minimal sanity tool for contributors without IAM creds).

1. `[x]` **Scaffold from template** — clone `AccelByte/extend-service-extension-go`, `rm -rf .git`, `git init`, push to `github.com/anggorodewanto/playtesthub`. Rewrite the `go.mod` module path + all internal imports to `github.com/anggorodewanto/playtesthub`. Strip the CloudSave `pkg/storage`; add `migrations/`, `pkg/repo/`, `pkg/config/`; confirm the gRPC + grpc-gateway skeleton builds and boots.
2. `[x]` **Proto `playtesthub.v1`** — define M1 RPCs from PRD §4.7: `GetPublicPlaytest` (unauth), `GetPlaytestForPlayer`, `AdminGetPlaytest`, `ListPlaytests`, `CreatePlaytest`, `EditPlaytest`, `SoftDeletePlaytest`, `TransitionPlaytestStatus`, `Signup`, `GetApplicantStatus`. Wire protoc codegen (via `proto.sh`) + `buf lint`. No handlers yet.
3. `[x]` **Postgres schema — migration 0001** — `Playtest`, `Applicant`, `Code`, `leader_lease`, `AuditLog` tables per [`schema.md`](schema.md). Indexes included. `pkg/migrate` runner + testcontainers-postgres integration test land with the migration; `scripts/smoke/boot.sh` verifies the binary boots end-to-end, applies migrations, exposes all 10 RPCs via gRPC reflection + the OpenAPI gateway, and reaches the handler layer. GitHub Actions `migrate up` gate is deferred to phase 12 with the rest of the CI workflow file.
4. `[x]` **Repository layer (`pkg/repo`)** — one package member per table with an interface + Postgres implementation. **Integration tests against testcontainers-postgres** ([`engineering.md`](engineering.md) §3.2) cover: slug uniqueness across soft-deleted rows, `UNIQUE (playtestId, value)` on Code, audit-log JSONB round-trip, status-transition CAS.
5. `[x]` **Config + IAM wiring** — `pkg/config` env-var parser (PRD §5.9 required vars); `pkg/iam` JWT validator wrapping the AccelByte Go SDK; AGS admin `sub` → `actorUserId` plumbed through request context.
6. `[x]` **Playtest RPCs — service layer** — per-RPC red-green: `CreatePlaytest` (STEAM_KEYS only; AGS_CAMPAIGN returns `Unimplemented`), `EditPlaytest` (whitelist enforcement), `SoftDeletePlaytest`, `TransitionPlaytestStatus`, the three `Get` variants, `ListPlaytests`. Every error row in [`errors.md`](errors.md) for these RPCs asserted in a handler test.
7. `[x]` **Signup RPC** — `Signup` + `GetApplicantStatus`. Discord handle lookup (`pkg/discord`) via bot token at signup, fallback to raw Discord ID on failure (PRD §10 M1). Signup idempotency (`(playtestId, userId)` natural key).
8. `[x]` **Extend App UI skeleton** — scaffolded `admin/` off `AccelByte/extend-app-ui-templates@templates/react` (the bundled CLI scenario list has no "Extend App UI" entry — direct git clone required). Tournament placeholder stripped. Vitest + React Testing Library wired (template ships no runner). `swaggers.json` points at the deployed backend URL (`<AGS_BASE_URL>/ext-<ns>-<app>/apidocs/api.json`); `npm run codegen` emits `src/playtesthubapi/`. Playtest list + create/edit (+ soft-delete) pages live in `admin/src/federated-element.tsx` against the generated react-query hooks; distribution-model selector shows AGS_CAMPAIGN visibly disabled with an M2 tooltip. Cloud deploy path validated end-to-end: `extend-helper-cli create-app` → `update-var`/`update-secret` → `image-upload --login` → `deploy-app --wait` — backend live at the deployed URL and `scripts/smoke/cloud.sh` (new) passes against it. **AppUI upload validated on Internal Shared Cloud** against namespace `abtestdewa-pong` via the dev `extend-helper-cli` binary (distributed on Google Drive per the AGS "Admin Portal Extend App UI Playtest" runbook); `appui create` + `appui upload` succeed with the helper-cli IAM client granted `ADMIN:NAMESPACE:{namespace}:EXTEND:APPUI` [CREATE, READ, UPDATE, DELETE]. **Admin Portal → backend data path validated**: UI mounted under **Extend → My Extend Apps → App UI**, `ListPlaytests` returns 200 with real data. Getting there required (a) bumping `accelbyte-go-sdk` v0.85.0 → v0.87.1, (b) teaching `pkg/common/gateway.go` to forward the HTTP `Cookie:` header as gRPC metadata and `pkg/common/authServerInterceptor.go` to extract the `access_token` cookie as a bearer-token fallback (the Admin Portal ships its session token in an httpOnly cookie, never as `Authorization:` — cookie path is the intended Extend App UI auth signal, confirmed by Try Aji), and (c) swapping the `option (playtesthub.v1.resource)` strings on admin RPCs from `CUSTOM:ADMIN:NAMESPACE:{namespace}:PLAYTEST` to `ADMIN:NAMESPACE:{namespace}:EXTEND:APPUI` because AGS Admin Portal in ISC does not yet let game admins assign `CUSTOM:*` perms to their own user roles (revert once the user-permission-management feature ships — see `docs/engineering.md` §8). **Still pending for public self-hosted audience**: the `appui` subcommand has not landed in a public `extend-helper-cli` release (latest v0.0.10) and AppUI remains Internal-Shared-Cloud-only per PRD §9 R11; re-attempt the public path when AGS ships the subcommand and extends the AppUI backend to customer-facing namespaces.
8.1. `[x]` **Phase 1-8 review fixes** — batch fix for spec-drift findings from the post-phase-8 review before phase 9 lands. In scope:
    - **[critical] Stop leaking NDA text to logs.** `main.go:124` currently passes `logging.PayloadReceived` + `logging.PayloadSent` to `logging.WithLogOnEvents`, so every `CreatePlaytest`/`EditPlaytest` request and every `GetPlaytestForPlayer` response dumps full `nda_text` into the JSON log stream — direct violation of PRD §6 ("NDA text, survey free-text answers, Code values MUST NOT appear in logs"). Same mechanism would leak `Code.value` in M2. Drop both payload events from `WithLogOnEvents`; keep `StartCall` + `FinishCall`. Add a smoke assertion: `CreatePlaytest` with a distinctive NDA marker string, grep logs for its absence.
    - **[high] Close the soft-cap bypass.** `pkg/service/playtest.go:113` queries `List(ctx, ns, includeDeleted=false)` for the 100-playtest cap check. Slugs stay reserved across soft-deletes (PRD §5.1) but the cap doesn't count them — create-then-soft-delete churn bypasses the cap and burns the slug namespace. Fix by counting all rows (either `includeDeleted=true` on the existing call or a new `CountAll` repo method). Integration test: 50 live + 50 soft-deleted → 101st create returns `ResourceExhausted`.
    - **[high] Fix `EditPlaytest` partial-update semantics.** `pkg/service/playtest.go:187-195` unconditionally overwrites every editable column from the request and always recomputes `CurrentNDAVersionHash`. The HTTP annotation is PATCH, so a `{"title":"new"}` body clears `nda_text`, `description`, `platforms`, `starts_at`, `ends_at`, and re-hashes NDA → every approved applicant bounces into re-accept for no reason (PRD §5.3: "version change forces re-accept"). Decide semantics: either document + test "all mutable fields required on every call" (reject empty title etc., matches current wire format), or add a `google.protobuf.FieldMask` and overwrite only masked fields. Either way: only recompute `CurrentNDAVersionHash` when `NDAText` actually changed vs. `current.NDAText`. Add a test that un-set fields survive a partial edit.
    - **[high] Extend `scripts/smoke/cloud.sh` to cover phase 7/8 RPCs.** Currently probes only `GetPublicPlaytest` (404) + `ListPlaytests` (401). Phase 8's whole premise is the cookie-forwarded Admin Portal auth path, which only exists on the deployed surface — `boot.sh` can't test it. Add unauth 401 probes for `POST /v1/player/playtests/{slug}/signup` and `GET /v1/player/playtests/{slug}/applicant`; if an AGS admin cookie is exportable via env, a cookie-authed `ListPlaytests` → 200 assertion too. Keeps STATUS.md smoke-policy honest ("phases 6, 7, 8, 9 all carry this sub-deliverable").
    - **[medium] Reject `NdaRequired=true` + empty `NdaText`.** `hashNDA("")` returns `""`; player sees an empty modal + infinite re-accept loop. Validate in `CreatePlaytest`/`EditPlaytest` with `InvalidArgument("nda_text required when nda_required=true")`; add a handler test + an `errors.md` row.
    - **[medium] Lock the `EditPlaytest` immutable-field contract down.** `errors.md` row for "attempt to edit immutable field" relies on those fields not being on the wire. Add a reflective test over `EditPlaytestRequest.ProtoReflect().Descriptor().Fields()` asserting the set equals the documented mutable whitelist — catches accidental proto drift (e.g. a future `optional string slug` field) before it ships silent-drop behavior. Also assert the `TransitionPlaytestStatus` rejection message names both the current and requested state.
    - **[medium] Replace hand-rolled cookie parser.** `pkg/common/authServerInterceptor.go:188-207` splits on `;` and returns everything after `=` unmodified — misses RFC 6265 quoted values and other edge cases. Use `net/http`'s parser (construct a throwaway `http.Request` with the Cookie header and call `.Cookie("access_token")`). Add a test with a quoted value.
    - **[low] Reject `initial_code_quantity` set on STEAM_KEYS.** Request field is silently dropped today; return `InvalidArgument("initial_code_quantity must not be set for STEAM_KEYS")` instead. Add handler test + `errors.md` row.

    Each sub-item lands in the same PR with its test + (where applicable) smoke-harness extension per repo TDD rule. No code change ships without red→green. Gates phase 9.
9. `[x]` **Player Svelte app skeleton** — `player/` with Svelte 5 + Vite + TypeScript + Tailwind v4. `src/lib/config.ts` hard-fails on every PRD §5.8 malformed branch (parse error, missing keys, non-URL `grpcGatewayUrl`/`iamBaseUrl`, empty `discordClientId`) and renders a dedicated boot-error screen. Hash routing (`#/playtest/<slug>`, `/signup`, `/pending`, `/callback`) so one bundle deploys to any static host with no server rewrites. Discord login is AGS IAM Authorization Code + PKCE via `idp_hint=discord`; tokens land in `sessionStorage` (smaller XSS blast radius than `localStorage`). Signup form collects only `platforms` (owned) per PRD §5.2; the Landing view is defensively asserted to never render `ndaText` even if the backend regresses and ships it. Vitest suite covers config validation, PKCE (including the RFC 7636 known vector), token exchange (IAM 5xx → "Login failed — please try again later"), API wrappers (URL shape, Bearer attach, 401 bubble), router, and all five routes — 53 tests green. New `scripts/smoke/player-build.sh` runs `npm install && npm run test && npm run build` and asserts the bundle shape; added to the repo smoke harness alongside `boot.sh`/`cloud.sh`. `.golangci.yml` excluded `player/` from Go lint paths to match the existing `admin/` treatment (keeps `flatted/golang/pkg/flatted/flatted.go` under npm deps out of the Go analysis set). NDA accept UI, code retrieval UI, and survey remain out of scope per PRD §4.1 (M2/M3). Real-AGS-IAM end-to-end login round-trip is verified only at unit level here — the live path gates on phase 10 registering the loopback redirect URI.
9.1. `[~]` **Discord-through-IAM URL shape + IAM client wiring** — the phase 9 skeleton composed `buildDiscordLoginUrl` as `{iamBaseUrl}/iam/v3/authorize?...&idp_hint=discord`, which was a guess at the AGS IAM endpoint for Discord federation. 9.1 verified the contract against the real `abtestdewa-pong` ISC tenant and wired the player to the corrected shape, but **the manual end-to-end browser smoke is blocked on shared cloud** — see 9.2. What landed:
    - **Verified URL shapes** against ISC: `/iam/v3/authorize` is **404**; the real OAuth2 authorize endpoint is `/iam/v3/oauth/authorize`. Token exchange path `/iam/v3/oauth/token` is correct. `idp_hint=discord` is a Keycloak-ism — AGS IAM silently ignores it; the docs section in `docs/engineering.md` flags this.
    - **Public IAM client registered** on `abtestdewa-pong` (`d6bb5dd2cf6b4d23bd6d6400d7886b94`, OAuth client type `Public`, PKCE-only, no secret). Backed-up by an empirical "`400 invalid_grant` from token endpoint without Basic auth" test that distinguishes a public client from a confidential one. Redirect URIs: `http://localhost:5173/callback`, `http://127.0.0.1/callback`. **Fragments are forbidden by AGS IAM** — `http://localhost:5173/#/callback` was silently invalid even after registration. Player switched from a hash-based callback (`#/callback`) to a path-based one (`/callback`); a one-shot `bridgePathCallback()` runs in `main.ts` before the app mounts and rewrites `/callback?code=…` to `/#/callback?code=…` so the existing hash-router's `Callback.svelte` still owns the route. New unit tests in `tests/bootstrap.test.ts`. 57 frontend tests green.
    - **`buildDiscordLoginUrl` + `exchangeCodeForToken` corrected** in `player/src/lib/auth.ts` to use `/iam/v3/oauth/authorize` and the verified token-endpoint body shape. Tests in `tests/auth.test.ts` updated to match.
    - **`player/public/config.json`** wired against `abtestdewa-pong`: `iamBaseUrl=https://abtestdewa-pong.internal.gamingservices.accelbyte.io`, `discordClientId=d6bb5dd2…`. `player/.env` switched to proxy through Vite at `/ext-abtestdewa-pong-playtesthub` so cross-origin browser policy doesn't fight the deployed backend.
    - **`docs/engineering.md`** got a new "Player AGS IAM client (Discord federation)" subsection — Admin-Portal click-path, redirect URI rules, public-vs-confidential probe, and the verified URL shapes. **Caveat**: that section's claim that `idp_hint=discord` triggers Discord federation is wrong on shared cloud; 9.2 rewrites it to the proxy-RPC shape.
    - **Admin UI status-transition affordance landed mid-stream** — phase 8's admin UI shipped Create/Edit/Delete but no DRAFT→OPEN button. Without that the player Landing view 404s every freshly created playtest (only OPEN playtests are public). `admin/src/federated-element.tsx` now renders **Publish** on DRAFT rows and **Close** on OPEN rows, both via the existing `TransitionPlaytestStatus` RPC. 10/10 admin tests green; bundle re-uploaded via the dev `extend-helper-cli`. Strictly a phase 8 follow-up — surfaced under 9.1 only because it blocked the smoke.

    **Why the manual smoke moves to 9.2**: AGS IAM's hosted `/auth/?request_id=…` page does not render the Discord button on shared cloud (per AccelByte docs: ["Set up a web login for Discord"](https://docs.accelbyte.io/gaming-services/modules/foundations/identity-access/authentication/discord-identity/#set-up-a-web-login-for-discord)). Confirmed empirically — the public 3rd-party platform list `/iam/v3/public/namespaces/{namespace}/platforms/clients/active` returns Discord with `IsActive=true` but the SPA still does not show it. Self-hosted apps must build their own login surface and drive AGS's `/iam/v3/oauth/platforms/discord/authorize` directly. That work is 9.2.

    Out of scope for 9.1 (and 9.2): NDA accept UI, code retrieval UI, survey (still M2/M3).

9.2. `[~]` **Self-hosted Discord login (build-our-own login page, shared-cloud-compatible)** — replaces the AGS-hosted `/auth/` redirect with a player-side flow that calls `/iam/v3/oauth/platforms/discord/authorize` directly. AGS IAM requires a `request_id` from a prior `/iam/v3/oauth/authorize` call to drive that endpoint, and the 302 from `/oauth/authorize` is an opaque redirect cross-origin (browser cannot read the `Location` header). Step 1 runs server-side via the new `Player.GetDiscordLoginUrl` proxy RPC. **Endpoint shape verified end-to-end against `abtestdewa-pong` ISC** before any code landed: `/iam/v3/oauth/authorize` 302s with `request_id`, and `/iam/v3/oauth/platforms/discord/authorize?request_id=…` 302s straight to `discord.com/api/oauth2/authorize` — no Admin Portal feature flag in the way. Code landed; manual browser smoke pending. Scope:
    - **New backend RPC `Player.GetDiscordLoginUrl`** — public/unauth, on the `Player` proto service. Request:
        ```proto
        message GetDiscordLoginUrlRequest {
          string redirect_uri          = 1; // must be on the player IAM client's allowlist
          string state                 = 2; // opaque, returned to the redirect_uri
          string code_challenge        = 3; // PKCE per RFC 7636
          string code_challenge_method = 4; // "S256"
          string scope                 = 5; // e.g. "account commerce social publishing analytics"
        }
        message GetDiscordLoginUrlResponse {
          string login_url = 1; // navigate the browser to this URL
        }
        ```
        HTTP annotation: `POST /v1/player/discord/login-url`. No `userId` in request — pre-auth.
    - **Handler**: server-side `http.Get` with `CheckRedirect = http.ErrUseLastResponse` (manual redirect handling) against `${AGS_BASE_URL}/iam/v3/oauth/authorize?response_type=code&client_id=${PLAYER_IAM_CLIENT_ID}&redirect_uri=${redirect_uri}&state=${state}&code_challenge=${code_challenge}&code_challenge_method=${code_challenge_method}&scope=${scope}`. Status MUST be 302; parse `Location` header. Reject if Location's query carries `error=…` (e.g. `error_description=redirect URI invalid` → `InvalidArgument`; `error=server_error` → `Unavailable`). Extract `request_id` from Location. Return `${AGS_BASE_URL}/iam/v3/oauth/platforms/discord/authorize?request_id=…&client_id=…&redirect_uri=…`. Response MUST NOT include the `state` or PKCE values — they remain in the player's `sessionStorage` (security: the verifier never traverses the wire).
    - **New env var `PLAYER_IAM_CLIENT_ID`** in `pkg/config` (PRD §5.9 add). The backend's confidential `AGS_IAM_CLIENT_ID` is **not** the same client; `d6bb5dd2…` is. Smoke harness must export both. Document in `docs/engineering.md` and `.env.template`.
    - **Errors `errors.md` rows**: `GetDiscordLoginUrl` failures — `InvalidArgument` (bad redirect_uri / missing PKCE / bad state shape), `Internal` (AGS IAM unreachable), `Unavailable` (AGS IAM 5xx). Byte-exact strings per the existing `errors.md` convention.
    - **Player change**: in `player/src/routes/Landing.svelte::handleSignup`, replace the local `buildDiscordLoginUrl(...)` call with `await fetchDiscordLoginUrl(config, { state, codeChallenge, redirectUri, scope })` (new helper in `src/lib/auth.ts`), then `window.location.href = response.login_url`. Drop `buildDiscordLoginUrl` and the `idp_hint=discord` query param entirely. `exchangeCodeForToken` and `Callback.svelte` are unchanged — they already handle the AGS-issued `code` correctly. Update `tests/auth.test.ts` to assert the new helper's request body + URL.
    - **Smoke harness**: extend `scripts/smoke/cloud.sh` with one POST to the new RPC asserting (a) 200, (b) response body's `login_url` contains `oauth/platforms/discord/authorize?request_id=` and the player's `client_id`. No live IAM round-trip — that's manual.
    - **Engineering doc rewrite**: replace the Discord URL-shape table in `docs/engineering.md` to describe the proxy-RPC flow honestly. Drop the now-misleading mention of `idp_hint=discord`.
    - **Verification**: manual smoke — open `http://localhost:5173/#/playtest/<seeded-slug>`, click Sign up, complete Discord auth (now on Discord's domain, not on AGS's `/auth/`), land back on `/callback`, see `/signup` render, submit, land on `/pending`, confirm the Applicant row in Postgres has `status=PENDING` and `userId` populated.

    Out of scope: NDA accept UI, code retrieval UI, survey (M2/M3). Out of scope for the player: any login surface for non-Discord platforms (covered in M3 if/when added). The backend RPC's design is generic enough to extend to other platforms later (the platform ID would just become a request field), but that's a future change.

    Risk: if AGS IAM's `/oauth/platforms/discord/authorize` itself is gated by some Admin Portal feature flag we haven't found, the whole approach fails. Mitigation: an early diagnostic curl reproducing the flow end-to-end with a stubbed `request_id` from `/oauth/authorize` — bail to a PRD-update conversation immediately if it errors, do not ship half a flow.

    **Outcome (2026-04-28): superseded by 9.3.** Code shipped but the live e2e flow rejects every `/iam/v3/oauth/token` exchange with `invalid_grant`. Root cause confirmed via AGS application log (trace `7ca156dae6e6402f94599163a209273d`, cluster `foundations-justice-internal`, namespace `justice`, log_type `application`):

    > `unable to generate token: invalid grant: invalid_grant: failed to load authorize data: internal error user justice platform account not found: a76d0b21f2554b758f8f996b972a4bf0`

    Discord federation succeeds and AGS issues an authorization code bound to the new user. On `/token`, AGS's `LoadAuthorize` (`pkg/oauth/model/jwtstore.go:232-246` in the local `~/projects/justice-iam-service` checkout) detects `isGameNamespace(publisher=foundations, ns=abtestdewa-pong) == true` and tries to look up the user's "justice platform account" in the game namespace. That record is created by `handleUserPlatformTokenGrantV3` (`pkg/oauth/api/v3handlers.go:2884` calls `createOrGetJusticePlatformAccount`) but **never** by `platformAuthenticateV3Handler` — so the PKCE auth-code flow leaves it absent and the next-step `/token` always fails. There is no client-side workaround: any auth-code grant in a game namespace via federated platform login on shared cloud hits the same failure. Moving the IAM client to the publisher namespace would skip the `isGameNamespace` branch but customers don't own `foundations` on shared cloud. Phase 9.3 switches to the platform-token grant flow, which AGS auto-creates the missing record on. Code from 9.2 (`Player.GetDiscordLoginUrl` proxy RPC, `PLAYER_IAM_CLIENT_ID` env var, `fetchDiscordLoginUrl` helper, `bridgePathCallback`, the smoke probe) is removed in 9.3 — only the PKCE machinery and the `discordClientId` config field stay (PKCE moves to a different role; see 9.3 below).

9.3. `[x]` **Discord federation via platform-token grant (replaces 9.2 architecture)** — switch from AGS's PKCE authorization-code federation to AGS's **platform-token grant** at `POST /iam/v3/oauth/platforms/discord/token`. That endpoint accepts `platform_token=<discord-auth-code>`, exchanges it with Discord using AGS's stored Discord client secret, auto-creates the missing Justice platform account in the game namespace, and returns an AGS access token in one round trip. No `/iam/v3/oauth/authorize` hop, no `request_id`, no AGS-issued auth code, no PKCE.

    **Architecture**: player runs Discord OAuth directly (Discord developer portal is the registered redirect-URI authority, not AGS), then hands the resulting Discord code to our backend, which uses confidential AGS creds to call the platform-token grant.

    1. Player navigates to `https://discord.com/oauth2/authorize?response_type=code&client_id=${DISCORD_CLIENT_ID}&redirect_uri=${PLAYER_ORIGIN}/callback&scope=identify+email&state=${state}`.
    2. Discord redirects to `${PLAYER_ORIGIN}/callback?code=<discord-code>&state=...` (no platform_id query, since this is direct Discord OAuth — not AGS-mediated).
    3. `Callback.svelte` reads `code` + `state`, validates `state` against `sessionStorage.pendingLogin`, POSTs `code` to a new backend RPC `Player.ExchangeDiscordCode`.
    4. Backend authenticates with `AGS_IAM_CLIENT_ID`/`AGS_IAM_CLIENT_SECRET` (Basic auth, confidential client) and calls `POST /iam/v3/oauth/platforms/discord/token` with `platform_token=<discord-code>` against the configured `${AGS_BASE_URL}`.
    5. AGS returns an access token + refresh token + expires_in. Backend forwards them to the player verbatim. Player stores `access_token` in `sessionStorage` (existing `setAccessToken` helper).

    **Scope:**
    - **Remove from phase 9.2:**
        - `Player.GetDiscordLoginUrl` RPC (proto + handler + tests).
        - `pkg/service/discord_login.go` (`DiscordLoginProxy`, `WithDiscordLoginProxy`, `buildAuthorizeURL`, `buildPlatformsDiscordURL`, `fetchRequestID`, `mapAuthorizeError`, `drain`).
        - `PLAYER_IAM_CLIENT_ID` env var from `pkg/config` and `.env.template` and `scripts/smoke/`.
        - `fetchDiscordLoginUrl` helper from `player/src/lib/auth.ts`.
        - The smoke harness probe in `scripts/smoke/cloud.sh` for `/v1/player/discord/login-url`.
        - The phase 9.2 debug `slog.InfoContext` calls in `discord_login.go` (whole file is removed).
        - `bridgePathCallback` in `player/src/lib/bootstrap.ts` and its `main.ts` call site — Discord redirects to `/callback?code=...` *as a query string*, but the player router is hash-based; rewrite to `/#/callback?...` is still needed. **Keep** `bridgePathCallback` (the comment justifying it is still valid; it's just no longer about AGS — it's about Discord). Update its top-of-file comment to drop the AGS-IAM specifics.
        - PKCE generation + `code_verifier`/`code_challenge` from the player's flow. Direct Discord OAuth doesn't use PKCE (Discord supports it but isn't required for confidential-server-side exchange, and we never expose the Discord secret to the player anyway). `sessionStorage` `pendingLogin` keeps `state` + `returnTo` only — drop `codeVerifier`. Update `tests/auth.test.ts` accordingly.
    - **Add:**
        - **New backend RPC `Player.ExchangeDiscordCode`** — public/unauth, on the `Player` proto service:
            ```proto
            message ExchangeDiscordCodeRequest {
              string code         = 1; // Discord OAuth authorization code
              string redirect_uri = 2; // MUST byte-exact match what player sent to discord.com/oauth2/authorize
            }
            message ExchangeDiscordCodeResponse {
              string access_token  = 1;
              string refresh_token = 2;
              int32  expires_in    = 3;
              string token_type    = 4; // always "Bearer"
            }
            ```
            HTTP annotation: `POST /v1/player/discord/exchange`. Pre-auth (no JWT validation). The `redirect_uri` field is required because AGS verifies it byte-exactly against what was sent to Discord — a mismatch is the most common debug-loop trap; surface it explicitly to keep the contract honest.
        - **Handler `pkg/service/discord_exchange.go`** — `DiscordExchangeProxy{ AGSBaseURL string; ClientID string; ClientSecret string; HTTPClient *http.Client }`. Builds Basic auth from `ClientID`+`ClientSecret`, posts `application/x-www-form-urlencoded` body `platform_token=<code>&redirect_uri=<...>` to `${AGSBaseURL}/iam/v3/oauth/platforms/discord/token`. The HTTP client is the *default* `http.Client` (follow redirects normally — there's no opaque-302 trick here). Map AGS responses:
            - 200 → forward `access_token`, `refresh_token`, `expires_in`, `token_type` verbatim.
            - 4xx with `{"error":"invalid_grant","error_description":...}` → gRPC `InvalidArgument` with the AGS `error_description` byte-exact (Discord code expired/already-used/invalid is the player's problem to fix by retrying).
            - 4xx with `{"error":"unauthorized_client",...}` → gRPC `Internal` ("backend Discord federation misconfigured") — logs+alerts but does not leak the AGS error to the player.
            - 5xx → gRPC `Unavailable` ("AGS IAM unreachable").
            - Network error → `Unavailable`.
        - **Wire in `main.go`** alongside the existing service-construction site. Remove the `WithDiscordLoginProxy` call; add `WithDiscordExchangeProxy` (same plumbing convention).
        - **Reuse existing env vars `AGS_IAM_CLIENT_ID` + `AGS_IAM_CLIENT_SECRET`** (already required for the IAM JWT validator). Do NOT introduce a new env var. The Basic auth on the platform-token grant uses the same confidential client.
        - **Player `Landing.svelte::handleSignup`** — replace the `fetchDiscordLoginUrl` flow with: store `pendingLogin = { state, returnTo }` (drop `codeVerifier`), then `window.location.href = "https://discord.com/oauth2/authorize?response_type=code&client_id=${config.discordClientId}&redirect_uri=${origin}/callback&scope=identify+email&state=${state}"`. The `discordClientId` field in `config.json` switches meaning: it's now the **Discord** OAuth client ID (e.g. `1495388556796100618`), not the AGS IAM client ID. Update `config.json.example`, `player/public/config.json`, the `Config` type's JSDoc, and `tests/config.test.ts` to reflect this. The leftover AGS public IAM client `d6bb5dd2cf6b4d23bd6d6400d7886b94` becomes unused and can be removed from the player config.
        - **Player `Callback.svelte`** — `params.code` is now the Discord code, not AGS's. Replace the `exchangeCodeForToken(config, ...)` call with `exchangeDiscordCode(config, { code, redirectUri })`. The new helper POSTs to `${grpcGatewayUrl}/v1/player/discord/exchange` with JSON `{ code, redirect_uri }` and returns the same `TokenResponse` shape. `redirectUri` is byte-exact `${origin}/callback`. Drop the `codeVerifier` read from `pendingLogin`.
        - **Player `auth.ts`** — `exchangeCodeForToken` removed; `exchangeDiscordCode` added. `setAccessToken` unchanged. `IamError` + `GENERIC_LOGIN_FAILED_MESSAGE` reused. PKCE helpers (`s256`, `randomVerifier`) removed.
        - **`docs/errors.md` rewrite** for the new RPC — five rows replacing the 9.2 rows: `ExchangeDiscordCode` `InvalidArgument` (missing/invalid code/redirect_uri), `InvalidArgument` (AGS rejected with invalid_grant — AGS message passed through), `Internal` (AGS misconfig), `Unavailable` (AGS 5xx / unreachable), `Unauthenticated` (Discord code rejected by Discord — surfaced from AGS as `invalid_grant`, intentionally same shape as the byte-exact AGS pass-through).
        - **Smoke harness extension** — replace the `/v1/player/discord/login-url` probe in `scripts/smoke/cloud.sh` with a `/v1/player/discord/exchange` probe. The probe sends an obviously-fake `code=smoke-bogus-${RANDOM}` and asserts a 400 response with `error="invalid_grant"` (AGS rejects bogus codes with that exact code). This validates: (a) RPC routed, (b) backend has working AGS Basic auth, (c) AGS responses mapped to gRPC codes correctly. Live success path is gated on a real Discord OAuth flow, which the bash harness can't do — covered by 9.4.
        - **`docs/engineering.md` rewrite** — replace the proxy-RPC flow table from 9.2 with a "Discord federation via platform-token grant" subsection. Document why the auth-code path doesn't work on shared-cloud game namespaces (link to the 9.2 outcome paragraph above). Move the URL-shape table.

    - **Verification**: red-green TDD per repo policy. Backend handler tests in `pkg/service/discord_exchange_test.go` cover happy path (mocked AGS 200), AGS `invalid_grant` passthrough, AGS `unauthorized_client` → `Internal`, AGS 5xx → `Unavailable`, network error → `Unavailable`, missing/invalid request fields → `InvalidArgument`. Player vitest covers the new `exchangeDiscordCode` helper (URL, body shape, error mapping) and the updated `Callback.svelte` route. Full smoke harness green: `scripts/smoke/boot.sh` + `scripts/smoke/cloud.sh` + `scripts/smoke/player-build.sh`. `go test ./...` and `golangci-lint run` clean. **Live e2e is 9.4** — do not mark 9.3 shipped on green tests alone.

    **Outcome (2026-04-28)**: code shipped. Backend RPC `Player.ExchangeDiscordCode` + `pkg/service/discord_exchange.go` landed with full unit coverage (12 tests). `pkg/service/discord_login.go` removed. `PLAYER_IAM_CLIENT_ID` env var dropped from `pkg/config`, `.env.template`, `scripts/smoke/env.sh`, `scripts/smoke/boot.sh`. Player rewired: `auth.ts` exports `buildDiscordAuthorizeUrl` + `exchangeDiscordCode`; PKCE module deleted; `pendingLogin` no longer carries `codeVerifier`; `Landing.svelte` redirects directly to `discord.com/oauth2/authorize`; `Callback.svelte` calls the new RPC. 57 player vitest pass. `discordClientId` in `player/public/config.json` switched semantics — now the Discord OAuth client ID (`1495388556796100618`), not the AGS public IAM client. `bridgePathCallback` retained (Discord still redirects to a path-based `/callback`); top comment updated. `docs/errors.md` rows replaced (5 → 5). `scripts/smoke/cloud.sh` probes `/v1/player/discord/exchange` with bogus code, asserts 400 invalid_grant. `docs/engineering.md` "Player AGS IAM client" subsection rewritten as "Discord federation via platform-token grant" with full flow table + setup checklist. `go test ./...` green, `golangci-lint run` clean (only preexisting `main.go` maintidx warning), `scripts/smoke/boot.sh` PASS, `scripts/smoke/env.sh` PASS, player `npm run test` + `npm run build` PASS. **Live e2e remains 9.4.**

    Out of scope: NDA accept UI, code retrieval UI, survey (M2/M3). Out of scope for the player: any login surface for non-Discord platforms (M3).

    Risk: if AGS's platform-token grant for the Discord platform is also gated by an `IsActive` flag on the platform credential (currently `IsActive=false` in `abtestdewa-pong` per the AGS access log seen during 9.2 debugging), the grant fails with a different error code. Mitigation: 9.5 documents the platform-credential setup; 9.4's first probe surfaces this immediately.

9.4. `[x]` **Phase 9 e2e re-verification** — drive the full Discord login flow end-to-end against the deployed backend, the same way 9.2 attempted before the architecture flip. Goal: prove a fresh user can sign in with Discord and see the player app's `/signup` (or `/playtest/<slug>` post-callback) without `Login failed`.

    Steps:
    1. **Activate the Discord platform credential in AGS Admin Portal.** Navigate to `https://abtestdewa.internal.gamingservices.accelbyte.io/admin/namespaces/abtestdewa-pong/login-methods/platforms/discord` and flip `IsActive` to `true`. Confirm via the IAM REST surface: `GET /iam/v3/admin/namespaces/abtestdewa-pong/platforms/all/clients` should return the Discord row with `IsActive: true`.
    2. **Confirm the Discord developer portal allowlist** for the Discord app (client_id `1495388556796100618`) includes `http://localhost:5173/callback`. The 9.2 debug session already added this; re-verify before clicking Sign up.
    3. **Boot the deployed backend** at the live `abtestdewa-pong.internal.gamingservices.accelbyte.io` URL with the 9.3 image. Local dev: `npm run dev` in `player/` against the Vite proxy at `/ext-abtestdewa-pong-playtesthub`.
    4. **Manual browser smoke**: open `http://localhost:5173/`, drive a fresh sign-up. Capture network: `POST /v1/player/discord/exchange` returns 200 with a non-empty `access_token`. Player navigates to `pendingLogin.returnTo` (root or signup page). `sessionStorage` carries the access token.
    5. **Repeat with a fresh tenant Chrome profile** (cleared cookies + sessionStorage) to confirm the first-time-user code path. AGS auto-creates the user + Justice platform account on this call — expect a measurable AGS log entry under trace ID for `createOrGetJusticePlatformAccount`.
    6. **Assert the AGS user shows up** in the Admin Portal under `abtestdewa-pong → Users` with the Discord platform link visible.
    7. **Mark 9.3 + 9.4 shipped** in this file. Update `docs/STATUS.md` M1 row notes. The smoke harness still runs against bogus codes; the live success path is captured here.

    Verification artifacts: a short markdown writeup in `docs/engineering.md` (or a new `docs/runbooks/discord-login.md`) documenting the verified flow with example payloads (no PII, no real tokens). Linkable from 9.5.

    If 9.4 fails: do **not** patch around the failure. Re-open 9.3 with the new evidence. The whole point of 9.3's structural change is to remove this class of failure; another `invalid_grant` here means the architecture is still wrong.

    **Outcome (2026-04-28)**: golden flow green end-to-end against `abtestdewa-pong` on image `v0.1.8-9.4`. Player launched at `http://localhost:5173/#/playtest/demo-01` → Sign up → Discord OAuth → `Callback.svelte` posted to `POST /v1/player/discord/exchange` → 200 with a valid AGS access token + refresh token (JWT decoded: `iss=https://internal.gamingservices.accelbyte.io`, `namespace=abtestdewa-pong`, `ipf=discord`, valid `sub`) → navigated to `#/playtest/demo-01/signup` → submitted `platforms=[STEAM, XBOX]` → `POST /v1/player/playtests/demo-01/signup` 200 → `GET /v1/player/playtests/demo-01/applicant` returned `status=APPLICANT_STATUS_PENDING` with `userId` matching the AGS JWT `sub`. M1 stop target met. Verification artifacts in `docs/runbooks/discord-login.md`.

    Three verification surprises that landed alongside 9.4 (each kept in scope because they were in the way of green):
    - **Handler bugfix `pkg/service/discord_exchange.go:mapAGSExchangeError`**: the 9.3 plan assumed AGS forwards Discord's 4xx `invalid_grant` as HTTP 4xx. AGS actually wraps it as HTTP 500 `error=server_error` with the Discord response embedded as a substring of `error_description` (e.g. `platform server error: unexpected HTTP status code response -- 6s -- https://discord.com/api/oauth2/token 400 {"error": "invalid_grant", ...}`). Patched the handler to detect that pattern and surface `InvalidArgument` so retries don't look like an outage to the player. Genuine AGS 5xx with no `discord.com`+`invalid_grant` marker still maps to `Unavailable`. New unit test `TestExchangeDiscordCode_AgsWrappedDiscordInvalidGrant_InvalidArgument`. New `docs/errors.md` row for the wrap. Smoke probe in `scripts/smoke/cloud.sh` retightened to assert the wrap is detected (`invalid_grant` marker in body).
    - **AGS Admin Portal Discord RedirectUri must byte-equal `${PLAYER_ORIGIN}/callback`**: AGS's platform-token grant ignores the `redirect_uri` we send in the form body. Verified via the upstream source path `pkg/oauth/api/v3handlers.go:2672` → `pkg/oauth/api/common.go:946` → `platforms/discord/discord.go:94`. AGS uses `platformClient.RedirectURI` from the Admin Portal value when it POSTs to `discord.com/api/oauth2/token`. Discord then compares that against the value the player sent to `/oauth2/authorize`. Mismatch → `400 invalid_grant: Invalid "redirect_uri" in request.` Documented in `docs/runbooks/discord-login.md` "Three URLs that MUST agree byte-for-byte". This is the load-bearing setup constraint phase 9.5's runbook will surface for new operators.
    - **`http://localhost:5173/callback` had to be added to the Discord developer portal** OAuth2 → Redirects allowlist for client `1495388556796100618`. Without it, `/oauth2/authorize` errors with "Invalid OAuth2 redirect_uri" before the player ever sees the consent screen.

    Two follow-ups that did NOT block 9.4:
    - **~~`Applicant.discordHandle=""` and `Applicant.platforms=[]` after a signup that selected `STEAM + XBOX`~~** — **resolved 2026-04-30 as not-a-bug.** Re-checked against `docs/schema.md` L88: `discordHandle` and `platforms` are admin-only fields; the player-facing `GetApplicantStatus` strips them by design (`pkg/service/applicant.go:165-190` `playerApplicantToProto` documented this; `applicant_test.go:89-98` already asserts the DB row carries the values **and** the player response strips them). The 9.4 verification observed the player response and concluded data was lost — the DB row is correct. Phase 11's e2e CLI test should assert against the admin RPC (which returns the full row) or directly against Postgres; the player view will continue to hide these fields. Adding a row to `setup-ags-discord.md` "Common failure modes" so future operators don't re-misread the response.
    - **First `POST /v1/player/discord/exchange` of a session sometimes returns HTTP 503** even though replaying the same code with `curl` seconds later returns 200 with a valid token. Suspected AGS Discord-call latency spike or vite dev-proxy short timeout on cold path; needs duration instrumentation around the AGS HTTP call to confirm. Second attempt always succeeded during the verification.

9.5. `[x]` **Game-developer setup runbook** — `docs/runbooks/setup-ags-discord.md` (new file). Step-by-step instructions a game developer running their own playtesthub instance must follow before the player Discord login works on their AGS tenant. Written assuming zero prior AGS knowledge; cross-referenced from `README.md` and `docs/engineering.md` so it's discoverable.

    Sections:
    - **Prerequisites**: AGS Shared Cloud or self-hosted AGS tenant; Admin Portal access with `ADMIN:NAMESPACE:{namespace}:CLIENT [CRUD]` permissions; a Discord application; ability to deploy to AGS Extend.
    - **Discord developer portal setup** (`https://discord.com/developers/applications`): create application → OAuth2 → add redirect URIs (Discord-side allowlist; the `${PLAYER_ORIGIN}/callback` for every environment that signs in users — `http://localhost:5173/callback`, the public production URL, and any preview/staging URLs). Note: Discord's redirect_uri match is byte-exact, including scheme, port, and absence of trailing slash. The 9.4 verification hit "Invalid OAuth2 redirect_uri" rendered on `discord.com/oauth2/authorize` — the symptom is rendered on Discord's domain *before* the consent screen, so the player never sees the failure landing back at the app. Capture `Client ID` (public, used in player config) and `Client Secret` (confidential, stored in AGS — not the playtesthub backend).
    - **AGS Admin Portal — Discord platform credential**: navigate to `Login Methods → Platforms → Discord` for the game namespace. Paste the Discord `Client ID` and `Client Secret`. Set `IsActive: true`. **Set `RedirectUri` to byte-exact `${PLAYER_ORIGIN}/callback`** — the same string the player will send to `discord.com/oauth2/authorize` (e.g. `http://localhost:5173/callback` for local dev, `https://playtests.example.com/callback` for prod). This is the load-bearing constraint discovered during 9.4: AGS's platform-token grant **ignores** the `redirect_uri` form-body parameter our backend sends and forwards the configured `platformClient.RedirectURI` to Discord's `/oauth2/token` instead. Discord then byte-compares against what the player sent at `/oauth2/authorize`; any mismatch fails with `400 invalid_grant: Invalid "redirect_uri" in request.` Verified against `~/projects/justice-iam-service` source: `pkg/oauth/api/v3handlers.go:2672` → `pkg/oauth/api/common.go:946` → `platforms/discord/discord.go:94` — no `redirectURI` is threaded through, so the Admin Portal value wins. **Implication for self-hosted operators**: one AGS Discord platform credential ⇒ one canonical `${PLAYER_ORIGIN}`. Dev (`http://localhost:5173/callback`) and prod cannot share unless they share an origin; document the per-environment override path explicitly. Save. Verify via `GET /iam/v3/admin/namespaces/{namespace}/platforms/all/clients` (returns the row with `IsActive=true` and the configured `RedirectUri`). The 9.5-runbook section MUST call out that **the AGS-doc default of `https://<your-ags-host>/iam/v3/platforms/discord/authenticate` is wrong for the playtesthub flow** — that value is for the AGS-mediated `/authorize`-redirect path which 9.3 abandoned; using it produces a redirect_uri mismatch on every login.
    - **AGS Admin Portal — IAM clients**: confirm `AGS_IAM_CLIENT_ID` (confidential, used by the backend for IAM JWT validation **and** the Discord platform-token grant) exists with permissions per the playtesthub deploy guide. The `discordClientId` in `player/public/config.json` is the **Discord** client ID, not an AGS IAM client. The 9.1-era public AGS IAM client (`d6bb5dd2…` style) is **not** required by the 9.3 flow — leave a "no longer used" note for upgraders.
    - **Playtesthub config wiring**: env vars (`AGS_BASE_URL`, `AGS_NAMESPACE`, `AGS_IAM_CLIENT_ID`, `AGS_IAM_CLIENT_SECRET`, `DISCORD_BOT_TOKEN` — bot token still required for handle lookup at signup per phase 7) — pointer to `.env.template` for the canonical list. `player/public/config.json`: `grpcGatewayUrl` (the deployed Extend backend URL), `iamBaseUrl` (the AGS host — used by SDKs/observability, not by the Discord exchange path), `discordClientId` (the Discord OAuth client ID).
    - **Verification ladder**: (a) `scripts/smoke/cloud.sh` against the deployed backend exits 0; (b) `scripts/smoke/cloud.sh` with a forced bogus exchange returns AGS `invalid_grant` (proves Basic-auth and routing are correct, even before any user exists); (c) Manual browser smoke completes per 9.4.
    - **Common failure modes & their fixes** (each row backed by a 9.4 reproduction; cross-link to `docs/runbooks/discord-login.md` "Failure modes seen during phase 9.4 verification" for the byte-exact error strings):
        - **`Invalid OAuth2 redirect_uri` on `discord.com/oauth2/authorize`** (rendered on Discord's domain, no callback fires): `${PLAYER_ORIGIN}/callback` is not on the Discord developer portal allowlist. Add it.
        - **`POST /v1/player/discord/exchange → 400`, body contains `discord.com/api/oauth2/token 400 {"error": "invalid_grant", "error_description": "Invalid \"redirect_uri\" in request."}`**: AGS Admin Portal Discord `RedirectUri` ≠ player's `${PLAYER_ORIGIN}/callback`. Set them byte-equal. This is the trap 9.4 hit when the AGS RedirectUri was the docs-default `/iam/v3/platforms/discord/authenticate` value.
        - **`POST /v1/player/discord/exchange → 400`, body contains `discord.com/api/oauth2/token 400 {"error": "invalid_grant", "error_description": "Invalid \"code\" in request."}`**: Discord code is bogus / already used / expired. The smoke probe deliberately produces this case to validate the AGS-server_error-wrapping detection in `mapAGSExchangeError`. For real users: retry — fresh OAuth roundtrip.
        - **AGS Discord platform `IsActive=false`**: AGS-side error before Discord is ever called. Flip the toggle in Admin Portal. Verify with the public probe `GET /iam/v3/public/namespaces/{namespace}/platforms/clients/active` — if Discord doesn't appear with `IsActive: true`, the toggle didn't persist.
        - **AGS returns `unauthorized_client`**: confidential client lacks the Discord-grant permission. Assign `NAMESPACE:{namespace}:USER:LOGIN [CREATE]` or equivalent per AGS's role catalogue.
        - **First `POST /v1/player/discord/exchange` of a session occasionally returns HTTP 503**: 9.4 saw this once; the same code replayed via `curl` returned 200 with a valid token seconds later. Suspected AGS Discord-call latency spike or vite dev-proxy short timeout. Tracked as a STATUS.md follow-up; document the workaround (retry once) here so operators don't escalate.
        - **`Applicant.discordHandle=""` after a successful signup**: Discord bot token unset, or AGS rate-limited the bot. Per PRD §10 M1, handle-lookup falls back to raw Discord ID on failure. 9.4 saw `discordHandle=""` (not even raw ID) — surface this as a phase 7 follow-up the operator should be aware of, not a blocker.
        - **`Applicant.platforms=[]` after submitting a non-empty selection**: 9.4 reproduction — separate phase 7 / phase 9 follow-up. Document as a known issue with a STATUS.md xref so operators don't think their setup is broken.
        Each row links to the AGS / Discord docs page where applicable, and to the `docs/runbooks/discord-login.md` failure-mode table for the byte-exact error strings to grep for.
    - **"Three URLs that MUST agree byte-for-byte" callout** — lift the table from `docs/runbooks/discord-login.md` so the runbook stands alone (Discord developer portal allowlist + player `buildDiscordAuthorizeUrl` redirect_uri + AGS Admin Portal Discord `RedirectUri`). 9.4 proved this is the single highest-leverage piece of context for a new operator: get all three byte-equal and the flow works; miss any one and Discord rejects with the specific error string documented under "Common failure modes".
    - **Verified-payload reference** — embed (or link to) `docs/runbooks/discord-login.md` "Verified payload shape" so operators know what a successful exchange returns and what AGS JWT claims to expect (`iss`, `namespace`, `parent_namespace`, `sub`, `client_id`, `scope`, `ipf=discord`, `union_id`, refresh-token's `platform_id=discord`). Useful for distinguishing "AGS issued a token but my downstream code rejected it" from "AGS issued no token".
    - **Cross-references**: `docs/PRD.md` §5.2 (Discord login user requirement), `docs/engineering.md` (Player AGS IAM client subsection), this STATUS.md phase 9.3 outcome paragraph (architecture rationale: why platform-token grant replaced the AGS-mediated `/authorize` flow), this STATUS.md phase 9.4 outcome paragraph (the handler patch + AGS-RedirectUri trap that gated green), `docs/runbooks/discord-login.md` (the descriptive end-to-end flow doc — this 9.5 runbook is its prescriptive companion), `docs/errors.md` rows for `ExchangeDiscordCode` (byte-exact wire contract).

    Out of scope: AGS tenant provisioning (assumed pre-existing); Discord bot setup beyond the OAuth app (the bot token for handle lookup is its own concern, documented separately in phase 7's deploy notes).

    Verification: a fresh contributor (or AI agent) following only this runbook can stand up a working Discord login on a clean AGS tenant. The runbook itself is the deliverable; success is measured by 9.4 being reproducible from it without prior context.

10. `[x]` **CLI harness (`pth`)** — `cmd/pth/` binary per [`cli.md`](cli.md). Real-IAM only; no fake-JWT path. Ships meta commands (`doctor`, `describe`, `version`, `--dry-run`); the auth group (`login --discord` loopback + `--manual` fallback, `login --password`, `logout`, `whoami`, `token`) with credential store at `~/.config/playtesthub/credentials.json` and refresh-token rotation; the user group (`create`, `delete`, `login-as`) wrapping AGS IAM admin endpoints; every M1 playtest/applicant subcommand (§6.1); and the `pth flow golden-m1` composite. `describe` catalogue diff-checked in CI. Consumed by the M1 e2e test (phase 11). **Setup prerequisite**: register `http://127.0.0.1/callback` (no port) as an allowed redirect URI on the playtesthub IAM client. AGS IAM ignores port + allows http/https interchange for loopback hosts, so one registration covers every ephemeral port the CLI picks.

    Sliced into 6 sub-phases (matches the 9.x cadence — each merge boot-clean, single-PR-reviewable, ships its own smoke extension). Sub-phase numbers gate sequentially: 10.1 must be green before 10.2 starts, etc. Phase 10 marks shipped only after 10.6 lands.

10.1. `[x]` **CLI skeleton + meta commands + first unauth RPC** — `cmd/pth/main.go` with global-flag parser (`--addr`, `--base-path`, `--namespace`, `--profile`, `--token`, `--anon`, `--timeout`, `--insecure`, `-v` per [`cli.md`](cli.md) §4), gRPC client construction (TLS-on by default, `--insecure` for loopback), JSON-on-stdout / gRPC-status-on-stderr output writer + exit-code mapping (§8), and the dispatch shell. Ships:
    - `pth version` — prints build SHA, proto schema version, Go version (linker-stamped via `-ldflags`).
    - `pth doctor` — connectivity probe: dials `--addr`, calls `GetPublicPlaytest` on a sentinel slug (`__pth_doctor__`), reports `OK` + latency on `NotFound` (handler reachable) or fails with the gRPC code.
    - `pth playtest get-public --slug <s>` — first end-to-end RPC wrapper. Implies `--anon`.
    - `--dry-run` plumbing on the get-public command (prints request JSON, no dial). Establishes the pattern reused by every later subcommand.

    Out of scope: any auth-requiring RPC, credentials store, `describe`. Smoke harness extension: new `scripts/smoke/pth.sh` builds the binary, runs `pth version`, `pth doctor` against the deployed cloud backend (asserts `OK`), and `pth playtest get-public --slug nonexistent` (asserts NotFound + exit 1). Wired into the existing smoke runner. Unit tests cover flag parsing, exit-code mapping, output formatting, dry-run.

    **Outcome (2026-04-30)**: shipped. New tree under `cmd/pth/` (`main.go`, `globals.go`, `output.go`, `version.go`, `doctor.go`, `playtest.go`) + 6 `*_test.go` files. Hand-rolled global-flag parser (stdlib `flag` doesn't stop at the first non-flag token cleanly) — flag/env/default precedence per cli.md §4. gRPC dial defaults to insecure on loopback (`localhost`, `127.0.0.1`, `::1`, empty) and TLS otherwise. Exit codes per cli.md §8: 0 OK, 1 client errors (NotFound/InvalidArgument/etc.), 2 transport (Unavailable/DeadlineExceeded), 3 local (flag/env/parse). `--dry-run` prints the proto request body via protojson and never dials. `pth doctor` accepts NotFound *or* InvalidArgument as proof a handler ran (the sentinel slug `__pth_doctor__` fails the PRD §5.1 regex upstream of the DB, so InvalidArgument is the realistic case once validation lands). One in-source seam — `playtestClientFactory` — makes the subcommands testable without dialling; production wires it through `(*Globals).dial`. **Smoke**: `scripts/smoke/pth.sh` brings up its own ephemeral Postgres + auth-disabled backend on the same `:6565` constant `boot.sh` uses (no env override exists yet — they run sequentially); asserts version JSON has `buildSHA`/`goVersion`/`protoSchema`/`protoFileCount`, doctor returns `status=OK`, get-public on a missing slug exits 1 with `gRPC NotFound` on stderr, and `--dry-run` against an unresolvable host prints the request JSON without erroring. **Verification**: `go test ./...` PASS, `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS, `scripts/smoke/env.sh` PASS, `scripts/smoke/pth.sh` PASS. **One scope deviation from the spec line "deployed cloud backend"**: phase 10.1's smoke targets a local ephemeral backend instead. Reason: `pth` talks gRPC directly on `:6565` (cli.md §3), and the AGS Extend deploy at `<AGS_BASE_URL>/ext-<ns>-<app>` exposes only HTTPS REST through the gateway. The native gRPC port isn't reachable from the public internet. Cloud-targeted CLI smoke needs an SSH tunnel or a separate gRPC ingress — out of scope for 10.1; revisit if/when the deploy surface gains a gRPC endpoint.

10.2. `[x]` **Auth group (password) + credentials store** — credential store at `~/.config/playtesthub/credentials.json` (perms-checked per [`cli.md`](cli.md) §7.3); `pth auth login --password --username <u> [--password-stdin]` against AGS IAM ROPC grant; `pth auth logout`; `pth auth whoami`; `pth auth token`; refresh-token rotation (auto-refresh when within 60s of expiry; clear error + remediation message on refresh failure). Token resolution order from §4 honoured: `--token` > `PTH_TOKEN` > store. Authenticated calls now flow through every subcommand via `--profile`. Discord login deferred to 10.3.

    Smoke extension: `scripts/smoke/pth.sh` adds a password-login probe gated on `PTH_E2E_USERNAME`/`PTH_E2E_PASSWORD` env vars (skip when unset so the smoke still runs in CI without secrets). Asserts `whoami` returns the expected `userId` post-login. Unit tests cover store read/write, perms enforcement, refresh-token rotation logic with a mocked IAM endpoint, password-via-stdin (no leak to argv/log).

    **Outcome (2026-04-30)**: shipped. New files under `cmd/pth/`: `credstore.go` (atomic writes, 0600 file / 0700 dir perms enforcement, refuses looser perms with a `chmod` remediation; `XDG_CONFIG_HOME` + `PTH_CREDENTIALS_FILE` overrides; Windows POSIX-perm carve-out), `iamclient.go` (AGS IAM v3 ROPC + refresh-token grants over `POST /iam/v3/oauth/token` with HTTP Basic; rich `*iamError` carries `error`/`error_description`/status code; `IsInvalidGrant` shorthand for the user-actionable case), `auth.go` (subcommand dispatcher + `runAuthLogin`/`Logout`/`Whoami`/`Token`; password reader is a swappable seam — `--password-stdin` reads one line from stdin, default opens `/dev/tty` and disables echo via `golang.org/x/term`), plus per-file `*_test.go` (≈1100 LOC of tests). `globals.go` grew a `tokenResolver` field + `resolveBearer` / `defaultTokenResolver` plumbing — `dial()` now consults the credential store after `--token`/`PTH_TOKEN`, refreshes on near-expiry, and persists the rotated tokens. Refresh leeway is 60s per spec. **Token-resolution precedence on the dial path**: `--anon` short-circuit → `--token`/`PTH_TOKEN` → store-keyed lookup `(addr, namespace, profile)` (mismatched addr or namespace returns no token, falling through to anon — keeps stale credentials from leaking across environments). **Spec deviation**: cli.md §4 doesn't list AGS IAM bootstrap config as a global flag, so the auth subcommand reads `PTH_AGS_BASE_URL` / `PTH_IAM_CLIENT_ID` / `PTH_IAM_CLIENT_SECRET` from env directly; documented in `pth --help`. **Smoke**: `scripts/smoke/pth.sh` got a gated probe (login → whoami → token → logout → post-logout whoami exits non-zero) behind `PTH_AGS_BASE_URL` + `PTH_IAM_CLIENT_ID` + `PTH_E2E_NAMESPACE` + `PTH_E2E_USERNAME` + `PTH_E2E_PASSWORD`; an isolated `PTH_CREDENTIALS_FILE` keeps the operator's real `~/.config/playtesthub` untouched. Probe skips cleanly when any env is unset (default CI path). **Verification**: `go test ./...` PASS, `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS, `scripts/smoke/env.sh` PASS, `scripts/smoke/pth.sh` PASS (auth probe correctly skipped — no live IAM creds in this environment). New transitive direct dep: `golang.org/x/term v0.40.0` (already indirectly required). **Live ROPC e2e is implicitly 10.4**: a `pth user create`'d native user is the only password-grant subject available without sharing a real human's AGS Admin Portal credentials. Until 10.4 lands, the auth probe stays gated.

10.3. `[x]` **Discord-federated login (loopback + manual fallback)** — `pth auth login --discord [--manual] [--no-browser]` per [`cli.md`](cli.md) §7.1. Mirrors the player's working flow rather than the AGS authorize-URL flow originally drafted: CLI loopback receives a **Discord** authorization code, then POSTs `{code, redirect_uri}` to the backend's `Player.ExchangeDiscordCode` (REST: `POST /v1/player/discord/exchange`) which runs the AGS platform-token grant. Architecture confirmed up-front based on phase 9.2/9.3 outcomes (AGS authorize-URL grant unavailable on shared-cloud game namespaces); no diagnostic step required. `--manual` skips the listener and prompts for the pasted redirect URL.

    Loopback uses a **fixed port** — `PTH_DISCORD_LOOPBACK_PORT` (default `14565`) — because Discord's redirect-URI allowlist is byte-exact including port (per [`docs/runbooks/discord-login.md`](runbooks/discord-login.md) "Three URLs that MUST agree byte-for-byte"); ephemeral ports would force the operator to re-allowlist on every login. Required CLI env: `PTH_DISCORD_CLIENT_ID`, `PTH_DISCORD_LOOPBACK_PORT`, `PTH_BACKEND_REST_URL` (HTTPS gateway base — gRPC's `:6565` doesn't carry the unauth REST mapping).

    Setup prerequisite (one-time): register `http://127.0.0.1:14565/callback` on **both** Discord developer portal → OAuth2 → Redirects **and** AGS Admin Portal → Login Methods → Platforms → Discord → RedirectUri. Documented in [`docs/runbooks/setup-ags-discord.md`](runbooks/setup-ags-discord.md) § "CLI loopback origin"; cross-link from the README dev-onboarding section once it lands in phase 12. Operators who want CLI Discord login *and* player Discord login on the same AGS tenant need either two AGS namespaces (each with its own Discord platform credential) or accept that only one of {player, CLI} is bootstrapped at a time per namespace — same trade-off the player flow already documents.

    Smoke extension: this path can't be exercised headlessly. Add a dry-run probe (`pth auth login --discord --no-browser --dry-run`) that prints the Discord authorize URL + listener address + exchange URL and exits 0 — proves URL construction + env-var plumbing without hitting Discord, the backend, or AGS. Manual verification documented in `docs/runbooks/setup-ags-discord.md` § "CLI loopback origin".

    **Outcome (2026-04-30)**: shipped. New `cmd/pth/discord.go` (~410 LOC) + `cmd/pth/discord_test.go` (~440 LOC) covering URL construction, env-var prechecks, dry-run for both modes, loopback happy/state-mismatch/missing-code/bind-failure paths, manual paste parsing (full URL + bare query + Discord error param), exchange POST 4xx/5xx/network failure mapping, and credential-store persistence with `loginMode="discord"`. `cmd/pth/auth.go` refactored: `runAuthLogin` pre-classifies the mode flag (`classifyLoginMode`) and dispatches to either `runAuthLoginPassword` (the existing 10.2 flow, factored out) or `runAuthLoginDiscord`. Mode flags `--discord` and `--password` are mutually exclusive; passing neither prints the new error message. Tests in `auth_test.go` updated to thread the new `envSnapshot` arg; the deferred-to-phase-10.3 placeholder test was replaced with a dispatch-wiring test + a mutual-exclusion test. **Spec deviation**: cli.md §7.1 originally called for AGS IAM `/authorize` PKCE; this phase ships the player-mirror flow up-front based on the 9.2/9.3 outcome. Fixed loopback port (`14565`) by design — Discord redirect-URI allowlist is byte-exact, so ephemeral ports would force re-allowlisting on every login. Required env vars `PTH_DISCORD_CLIENT_ID`, `PTH_DISCORD_LOOPBACK_PORT` (default 14565), `PTH_BACKEND_REST_URL` are documented in `cmd/pth/main.go` usage block + cli.md §7.1. **Smoke**: `scripts/smoke/pth.sh` got an unconditional dry-run probe asserting `mode=loopback`, `redirectUri=http://127.0.0.1:14565/callback`, `exchangeUrl=…/v1/player/discord/exchange`, and that the authorize URL contains `discord.com/oauth2/authorize`, the configured `client_id`, and a `state=` param. **Verification**: `go test ./...` PASS, `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS, `scripts/smoke/pth.sh` PASS. **Live e2e gating**: real Discord login round-trip needs the operator to register `http://127.0.0.1:14565/callback` on Discord's developer portal AND on AGS Admin Portal → Login Methods → Platforms → Discord → RedirectUri. Procedure documented in `docs/runbooks/setup-ags-discord.md` § 6 "CLI loopback origin"; that section also calls out the one-RedirectUri-per-namespace trap (player and CLI can't share a Discord platform credential without splitting namespaces).

10.4. `[x]` **User group (AGS IAM admin endpoints)** — `pth user create [--count <n>] [--country <iso2>]`, `pth user delete --id <userId> [--yes]`, `pth user login-as --id <userId> [--password-stdin]`. Wraps AGS IAM admin REST endpoints (not playtesthub RPCs); requires an admin-credentialled active profile. `--yes` required to skip the destructive prompt on `delete` so the e2e harness can run non-interactively but a stray invocation can't silently wipe a real user.

    `create` hits `POST /iam/v4/admin/namespaces/{ns}/test_users` — AGS generates username + password + email and marks the row as a test user (no verification email, distinct from production rows). Stdout shape: `{userId, username, password, emailAddress}` for `count=1`, `[ ... ]` for `count>1`. `login-as` looks up the username via `GET /iam/v3/admin/namespaces/{ns}/users/{userId}` then runs the existing ROPC grant; the caller pipes the AGS-generated password from `create`'s stdout into `login-as --password-stdin`. `delete` hits `DELETE /iam/v3/admin/namespaces/{ns}/users/{userId}/information` (the v3 endpoint AGS forwards the deprecated `/iam/namespaces/.../users/{id}` shape to).

    The native (non-federated) user has no Discord ID claim, so playtesthub `Signup` falls back to the raw IAM `sub` per PRD §10 M1 — that's the path the e2e suite uses (Discord-federated login isn't testable headlessly). Smoke extension: a create→login-as→delete round-trip gated on the same `PTH_E2E_*` admin creds as 10.2. Unit tests mock the AGS IAM admin REST surface and assert request shape (URL, headers, body, password-via-stdin).

    **Outcome (2026-05-01)**: shipped. New file `cmd/pth/user.go` (~250 LOC) with `runUser` dispatcher + `runUserCreate` / `runUserDelete` / `runUserLoginAs` + `requireAdminBearer` (resolves the active profile's bearer through `resolveActiveProfile`, so refresh-token rotation runs uniformly with every other auth path). `cmd/pth/iamclient.go` grew three admin methods — `adminCreateTestUsers`, `adminGetUserByID`, `adminDeleteUser` — plus `requireAdminConfig` / `doAdminJSON` shared transport (JSON in/out, bearer auth, parseIAMError reuse). One **spec deviation** vs the original phase line: switched from `AdminCreateUserV4` (caller-pinned username + password, but requires injecting fake `country`/`dateOfBirth` defaults) to `AdminCreateTestUsersV4` (AGS-generated username/password/email, marked as test rows so they're distinct from production). `cli.md` §6.1 user-group block + §11 e2e flow updated to match — caller now captures `{userId, username, password}` from `pth user create`'s stdout and pipes the AGS-generated password into `pth user login-as --password-stdin`. `pth user delete` refuses without `--yes` and exits 3 (local error) — scripted callers must opt in explicitly. **Smoke**: `scripts/smoke/pth.sh` got unconditional dry-run probes for all three subcommands (asserts URL + body + bearer shape without dialling AGS) plus a `--yes` guard test, and the gated `PTH_E2E_*` block grew a live create→login-as→delete round-trip that uses an isolated profile per created user. **Verification**: `go test ./...` PASS, `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS, `scripts/smoke/env.sh` PASS, `scripts/smoke/pth.sh` PASS (live user-group probe correctly skipped — no live AGS admin creds in this environment).

10.5. `[x]` **Playtest + applicant subcommands** — every M1 single-RPC wrapper from [`cli.md`](cli.md) §6.1: `playtest get-player`, `playtest get`, `playtest list`, `playtest create`, `playtest edit`, `playtest delete`, `playtest transition`, `applicant signup`, `applicant status`. Each command supports `--dry-run` (per the pattern from 10.1). Field shaping mirrors PRD-whitelisted mutables on `edit` (rejects non-whitelist flags client-side with a clear error before dialling).

    Smoke extension: `scripts/smoke/pth.sh` extends to a full create→list→transition OPEN→signup→status round-trip when the admin + player profiles from 10.2/10.4 are present (matches the golden-m1 shape but invoked as raw subcommands so flow composition lands cleanly in 10.6). Unit tests cover flag parsing for every command + the `edit` whitelist client-side rejection.

    **Outcome (2026-05-01)**: shipped. `cmd/pth/playtest.go` rewritten — 8 subcommands sharing a single `invokePlaytest` helper (dial + dry-run + timeout + gRPC→exit-code mapping in one place; `get-public` refactored onto it for consistency). New `cmd/pth/applicant.go` (`signup` / `status`) reuses the same helper through the existing `playtestClientFactory` seam, so the applicant commands ride on the same `runApplicant(ctx, ..., factory)` plumbing main.go wires to `(*Globals).dial`. Field-shaping helpers — `parsePlatforms`, `parseTimestampFlag`, `parseDistributionModel`, `parsePlaytestStatus`, `readMaybeFile` (the `@file` / `@-` stdin convention) — live next to their first use in `playtest.go`. `EditPlaytestRequest` is already proto-level whitelisted, but the spec called for a *clear, client-side* rejection: `editImmutableFlags` table + `detectImmutableEditFlags` scan emit a tailored error (e.g. `slug is immutable (PRD §5.1) — create a new playtest instead`) before any flag-parse runs, so the user sees *why* the field is rejected rather than a generic "unknown flag". **Spec deviation** vs cli.md §6.1 phrasing: applicant subcommands take `--slug` (not `--playtest <id>`) — the proto fields are `slug`, and `--slug` matches the existing `playtest get-public` precedent; cli.md updated to match. Unit tests in `playtest_test.go` (~430 LOC, 22 cases) + `applicant_test.go` (7 cases) cover happy paths, dry-run JSON shapes, every required-flag rejection, the immutable-edit gate (both `--slug` and `--status=OPEN`), bad platform/timestamp/distribution-model parsing, NDA `@file` loading, and short-form status normalization. **Smoke**: `scripts/smoke/pth.sh` got 11 unconditional dry-run probes (one per new subcommand + `--slug`/`--status` immutability + `transition --to ARCHIVED` rejection + `signup` missing-platforms guard); the gated `PTH_E2E_*` block now does an admin create→list→transition OPEN → player (smoke-test-$id) signup→status (asserts `APPLICANT_STATUS_PENDING`) → admin soft-delete round-trip with a per-run unique slug `pt-$(date +%s)-$$`. **Verification**: `go test ./...` PASS, `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS, `scripts/smoke/env.sh` PASS, `scripts/smoke/pth.sh` PASS (live playtest+applicant probe correctly skipped — no live AGS admin creds).

10.6. `[x]` **`describe` catalogue + `flow golden-m1` + CI diff-check** — `pth describe` emits the JSON catalogue (stable schema `cli-schema.v1`, [`cli.md`](cli.md) §5) of every subcommand built so far: name, milestone, required flags, optional flags, description, example. Catalogue is generated from a single source-of-truth registry in `cmd/pth/registry.go`; every subcommand from 10.1–10.5 gets one entry.

    `pth flow golden-m1 --slug <s>` composes the subcommands in-process (NDJSON output per [`cli.md`](cli.md) §8): create-playtest → transition OPEN → signup (synthetic player) → assert `status=PENDING`. Stops at first failure and exits non-zero. `--admin-profile` + `--player-profile` flags select the credential profiles to use.

    CI gate: a checked-in `cmd/pth/testdata/describe.golden.json` is regenerated on every PR and diff-checked (matches the admin-codegen gate pattern from [`engineering.md`](engineering.md) §5). Catches silent command-catalogue drift before AI consumers see it.

    Smoke extension: `scripts/smoke/pth.sh` adds `pth describe | jq '.commands | length'` (asserts >0) and a gated `pth flow golden-m1` round-trip when admin+player profiles are present. With this, 10.6's smoke is the de-facto e2e CLI exercise; phase 11's `e2e/*_test.go` suite is its programmatic counterpart.

    **Outcome (2026-05-01)**: shipped. New files under `cmd/pth/`: `registry.go` (~210 LOC — single source of truth, alphabetised `commandSpec` slice covering all 10.1–10.6 commands; helper `slugFlag`/`idFlag`/`platformsFlag`/`dryRunFlag` deduplicate the most common flag specs), `describe.go` (`runDescribe` + `describeOutput` shape — pretty-printed two-space JSON so the golden diff is byte-exact), `flow.go` (~250 LOC — `flowProfileFactory` seam returns `(playtestClientFactory, *Globals)` per profile so admin and player steps reuse the dial-time refresh path; `flowInvoke` consolidates dial+timeout+gRPC→exit-code mapping; three NDJSON line shapes (`flowSuccessLine`/`flowFailureLine`/`flowDryRunLine`) keep absent fields absent rather than `null`). `output.go` grew shared `statusOK` / `statusFailed` constants so doctor + flow agree without `goconst` flagging. `main.go` dispatches `describe` and `flow` with usage updated to "10.1–10.6". Tests: `describe_test.go` (5 cases — schema marker, name uniqueness, alphabetical order, all entries sane, byte-exact golden diff) + `flow_test.go` (~340 LOC, 9 cases — required-flag rejections, --dry-run emits all 4 step shapes without dialling, happy path, stop-on-transition-failure with player stub untouched, status-mismatch synthetic FAILED, dial-failure exits transport). **Spec deviation** vs phase line "every subcommand from 10.1–10.5 gets one entry": the catalogue also includes 10.6's own `describe` and `flow golden-m1` so `pth describe` is self-describing — strictly additive, doesn't break the named guarantee. **Smoke**: `scripts/smoke/pth.sh` got two unconditional probes (describe schema=cli-schema.v1 + non-empty commands; flow --dry-run emits 4 NDJSON steps with correct labels in order) + a gated live `flow golden-m1` round-trip in the existing `PTH_E2E_*` block (separate slug from the manual sequence; teardown via list+delete on slug match). `wc -l` line count uses `printf '%s\n'` so trailing-newline trimming from `$(...)` doesn't off-by-one. **Verification**: `go test ./...` PASS, `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS, `scripts/smoke/env.sh` PASS, `scripts/smoke/pth.sh` PASS (live flow round-trip correctly skipped — no live AGS admin creds).

    Phase 10 is shipped after 10.6 lands.
11. `[x]` **E2E golden flow (M1 stop)** — a top-level `e2e/` test that boots the backend in-process against testcontainers-postgres, points `pth` at the real configured AGS IAM (user's own namespace per `cli.md` §7.4), and drives `pth flow golden-m1` against a per-run throwaway test user provisioned via `pth user create`. Asserts the applicant row lands with `status=PENDING`; teardown deletes the test user and soft-deletes the test playtest. Slugs + usernames namespaced `e2e-<timestamp>-<random>` for collision safety.

    **Outcome (2026-05-01)**: shipped. New `internal/bootapp/` package wires the gRPC server (interceptors + service registration + reflection + health + Prometheus) so both `main.go` and the e2e suite construct identical servers. `bootapp.New(ctx, Options{Config, DBPool, Listener, Logger, HTTPClient})` returns a `*Server` with `Serve()` / `Stop()` / `Addr()`; `BuildOAuthService` is exported so main.go retains its `LoginClient` warmup outside bootapp. Out of scope (kept in main.go because e2e doesn't need them): grpc-gateway HTTP server, metrics endpoint, OTEL tracer, swagger UI, signal handling. main.go shrunk from 414 → 252 lines. Two unit tests in `internal/bootapp/bootapp_test.go` lock the required-options contract; the full serve→dial→handler→DB path is owned by the e2e suite where testcontainers gives us a real Postgres for free. New `e2e/main_test.go` + `e2e/golden_m1_test.go` (~370 LOC). `TestMain` is intentionally minimal — a lazy `getHarness(t)` spins testcontainers-postgres + applies migrations + boots bootapp on `127.0.0.1:0` (free port, parallel-safe alongside the smoke's hardcoded :6565) + builds the `pth` binary into a tempdir + isolates `PTH_CREDENTIALS_FILE` so the harness never touches the operator's real `~/.config/playtesthub`. The harness is built once on the first non-skipped test and torn down via `TestMain` after `m.Run`. Backend boots with `AuthEnabled=true` so it validates real AGS-issued JWTs against the operator's tenant. **Suite is gated** on six required env vars (`AGS_BASE_URL`, `AGS_NAMESPACE`, `AGS_IAM_CLIENT_ID`, `AGS_IAM_CLIENT_SECRET`, `E2E_USERNAME`, `E2E_PASSWORD`) plus two optional ones (`PTH_IAM_CLIENT_ID`, `PTH_IAM_CLIENT_SECRET`, both default to the AGS_IAM_CLIENT_* pair so single-confidential-client setups need no extra config). Any unset required var → `t.Skip` with a clear reason; CI without secrets stays green. `TestGoldenM1Flow` drives admin login → user create → user login-as → `pth flow golden-m1 --slug e2e-<unix>-<8hex> --admin-profile e2e-admin-<suffix> --player-profile e2e-player-<userId>` via `os/exec`, parses the 4-line NDJSON, asserts each step's `status=OK` in order (`create-playtest` → `transition-open` → `signup` → `assert-pending`), and belt-and-braces re-asserts the assert-pending response carries `applicant.status=APPLICANT_STATUS_PENDING`. `t.Cleanup` chains the playtest soft-delete (resolved from `create-playtest`'s response.playtest.id) and the test-user delete (`--yes`) so a partial-failure run still tears down. **Spec deviation** vs phase line "in-process": `pth` is invoked as a subprocess (it must be — it's a CLI binary with its own process boundaries), but the *backend* is in-process per the spec. **Verification**: `go test ./...` PASS (e2e correctly skips without env), `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS, `scripts/smoke/pth.sh` PASS, `scripts/smoke/env.sh` PASS. Live e2e (with secrets set) is the operator's verification — same env shape as the existing `scripts/smoke/pth.sh` gated block, so an operator who has run that successfully has the credentials needed for `go test ./e2e/...` too.
12. `[x]` **CI + README walkthrough** — every gate in [`engineering.md`](engineering.md) §5 live on the GitHub Actions workflow; README reproduces the golden flow on a clean checkout using `pth flow golden-m1`; MIT `LICENSE` at repo root.

    **Outcome (2026-05-02)**: shipped. Three deliverables:
    - **`.github/workflows/ci.yml`** — six parallel jobs covering every wired engineering.md §5 gate: `go-lint` (`golangci/golangci-lint-action@v6`, v1.62.2), `go-test` (`go test ./...` on `ubuntu-latest` — Docker daemon ships pre-installed for testcontainers; `pkg/migrate/migrate_test.go` doubles as the "Migrations apply" gate, so no separate `migrate up` job is needed), `proto-lint` (`buf lint` via `bufbuild/buf-setup-action@v1` pinned to 1.50.0 to match the Makefile), `proto-fresh` (installs protoc 28.3 + the four Go plugins, runs `./proto.sh`, asserts `git diff --exit-code` over `pkg/pb` + `gateway/apidocs`), `player` (`npm ci` + `npm run test` + `npm run build` in `player/`), `admin` (`npm ci` + `cg:clean-and-generate` + git-diff drift assert + `npm run test` + `npm run build`). **Spec deviations**: (a) `admin` uses `cg:clean-and-generate` instead of the full `codegen` chain because `cg:download` fetches from a private Internal-Shared-Cloud URL the runner can't reach; the swapped step still catches drift between checked-in `admin/swaggers/playtesthub.json` and `admin/src/playtesthubapi/`. (b) The Svelte a11y gate (`@axe-core/playwright`) is intentionally absent — no Playwright/axe scaffold exists in `player/tests/`, so wiring it would either green-light a no-op or hard-red CI. Tracked as phase 12.1 (M3-adjacent polish).
    - **`README.md`** rewritten — drops the inherited `extend-service-extension-go` template prose. New structure: one-paragraph project pitch + mermaid diagram, RBAC-not-production-safe call-out (PRD §10 M3 reminder), doc-source-of-truth table, Quick Start (clone → docker compose → smoke), **Reproduce the golden flow** section that walks `pth flow golden-m1` end-to-end with the expected 4-line NDJSON output, architecture-at-a-glance, dev workflow with the full verification checklist from CLAUDE.md, AGS deploy steps, contributing notes, license. Phase 12 line "README reproduces the golden flow on a clean checkout using `pth flow golden-m1`" satisfied.
    - **`LICENSE`** — `LICENSE.txt` renamed to `LICENSE` (no extension; GitHub-conventional, satisfies `MIT LICENSE at repo root`); copyright header swapped from `Copyright (c) 2023 AccelByte Inc` (template residue) to `Copyright (c) 2026 Anggoro Dewanto and playtesthub contributors`.

    **Verification**: `go test ./...` PASS (e2e suite skips without live AGS env, as designed), `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS, `scripts/smoke/env.sh` PASS, `scripts/smoke/pth.sh` PASS, `player/npm test` PASS, `player/npm run build` PASS. CI workflow itself is verified by syntax inspection only — first cloud run lands when this commit pushes to `main` / opens a PR.

13. `[ ]` **Player a11y gate + admin codegen-fresh gate (phase 12.1, deferred)** — two engineering.md §5 gates that phase 12 left unwired:
    - **Player a11y** — wire `@axe-core/playwright` over the five player pages (Landing, Signup, Pending, Callback, error/boot screen) with zero-critical-violation assertion scoped to `wcag2a, wcag2aa, wcag21a, wcag21aa`. Phase 12 shipped without it because no Playwright scaffold exists.
    - **Admin job (codegen-fresh + build + test)** — phase 12's first cloud run revealed that **both** `admin/swaggers/` and `admin/src/playtesthubapi/` are gitignored (`.gitignore` lines 251 & 252). The job has no usable prerequisites: no spec to regenerate from, no committed clients to compile against. Phase 12 settled by removing the admin job from `.github/workflows/ci.yml` outright; the admin frontend is exercised only by maintainers running `npm run test && npm run build` locally pre-merge. Three viable rewires for 12.1: (a) commit the generated `admin/src/playtesthubapi/` directly and accept the codegen churn in PR diffs, (b) commit `admin/swaggers/playtesthub.json` and run `cg:clean-and-generate` in CI, (c) repoint `admin/swaggers.json` at the proto-derived `gateway/apidocs/api.swagger.json` (already CI-deterministic via the `proto-fresh` job) and verify `accelbyte-codegen` accepts that shape. (c) is the cleanest because `gateway/apidocs/` is already the source of truth for the player REST surface and re-running `proto.sh` regenerates it deterministically; risk is `accelbyte-codegen`-specific OpenAPI annotations that the protoc plugin doesn't emit.
    Both gates are M3 polish (PRD §10 M3 README walkthrough cadence), not M1 golden-flow correctness — no behavioral risk in shipping M1 without them.

### M2 — NDA + approval + code grant + AGS Campaign API + RetryDM

Target: golden flow stops at "player sees code in UI" (PRD §4.1 step 7). 10 new RPCs: `AcceptNDA`, `ListApplicants`, `ApproveApplicant`, `RejectApplicant`, `RetryDM`, `GetGrantedCode`, `UploadCodes`, `TopUpCodes`, `SyncFromAGS`, `GetCodePool`. Same red–green TDD discipline + smoke-harness-lands-with-code rule as M1 ([CLAUDE.md](../CLAUDE.md), [engineering.md](engineering.md) §4).

Phase status legend: `[ ]` not started · `[~]` in progress · `[x]` shipped.

1. `[x]` **Proto `playtesthub.v1` M2 RPCs** — define the 10 RPCs above per PRD §4.7; regen Go stubs + grpc-gateway + OpenAPI (`gateway/apidocs/api.swagger.json`, served at `/apidocs/api.json`); `buf lint` clean; commit drift. No handlers yet — all return `Unimplemented` until their phase lands.

    **Outcome (2026-05-02)**: shipped. `proto/playtesthub/v1/playtesthub.proto` grew the 10 M2 RPCs (`AcceptNDA`, `GetGrantedCode`, `ListApplicants`, `ApproveApplicant`, `RejectApplicant`, `RetryDM`, `UploadCodes`, `TopUpCodes`, `SyncFromAGS`, `GetCodePool`) with full grpc-gateway HTTP annotations + OpenAPI summaries/descriptions, plus admin-side `(playtesthub.v1.action)` + `(playtesthub.v1.resource)` options matching the M1 `ADMIN:NAMESPACE:{namespace}:EXTEND:APPUI` pattern from STATUS.md M1 phase 8. New entities: `NDAAcceptance`, `Code`, `CodePoolStats`, plus `CodeState` enum (UNUSED/RESERVED/GRANTED — schema.md §"Code table"). 14 new req/resp messages; `UploadCodesResponse.rejections[]` is the structured per-line reject channel for the PRD §4.3 whole-file-reject path. `make lint-proto` clean (buf 1.50.0). `make proto` regenerates `pkg/pb/playtesthub/v1/{playtesthub,playtesthub_grpc,playtesthub.pb.gw}.go` + `gateway/apidocs/api.swagger.json` cleanly; the regenerated `playtesthub.pb.gw.go` registers gateway handlers for every new RPC (HTTP surface live at boot). **No handler edits**: `pkg/service/PlaytesthubServiceServer` already embeds `pb.UnimplementedPlaytesthubServiceServer`, so the 10 new methods auto-return gRPC `Unimplemented` until the gating phase implements each. Spec deviation vs the line "`apidocs/api.json`": no separate file is generated — the swagger spec lives at `gateway/apidocs/api.swagger.json` on disk and the backend serves it under `/apidocs/api.json` (see `main.go:290`); STATUS line clarified. **Verification**: `go test ./...` PASS (extended `pkg/service/service_test.go` `TestServiceDescriptorMethods` from 11 → 21 expected RPC names so future silent removals still trip), `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS (extended with two new probes — reflection now asserts all 21 RPCs visible, and an Unimplemented probe calls `GetGrantedCode` over the native gRPC port and asserts the response status is `Unimplemented`).
2. `[x]` **Migration 0002 — `NDAAcceptance` table + audit-action enum extension** — `NDAAcceptance(userId, playtestId, ndaVersionHash, acceptedAt)` with composite PK per [`schema.md`](schema.md); audit `action` enum gains `nda.accept`, `applicant.approve`, `applicant.reject`, `code.upload`, `code.upload_rejected`, `code.grant_orphaned`, `applicant.dm_sent`, `applicant.dm_failed`, `dm.circuit_opened`, `dm.circuit_closed`, `campaign.create`, `campaign.create_failed`, `campaign.generate_codes`, `campaign.generate_codes_failed`. Audit M1 migration's `Code` row to confirm `reservedBy`/`reservedAt`/`grantedCodeId`/`grantedAt` already present; add any missing columns in this migration (append-only — never edit 0001). testcontainers-postgres test asserts schema + JSONB round-trip for every new action.

    **Outcome (2026-05-02)**: shipped. New `migrations/0002_m2.{up,down}.sql` adds the `nda_acceptance` table with composite PK `(user_id, playtest_id, nda_version_hash)` + `accepted_at TIMESTAMPTZ NOT NULL DEFAULT NOW()` + FK to `playtest(id)`. **No Code-table changes needed**: `code.reserved_by`/`reserved_at`/`granted_at` and `applicant.granted_code_id` (the schema's `grantedCodeId` lives on Applicant, not Code) are all already present from migration 0001 — the STATUS line about "audit and add any missing" resolves to a no-op. **No DB-level audit-enum extension**: `audit_log.action` is plain TEXT with no CHECK constraint (deliberate — see migration 0001), so the action set is a code/doc contract enforced via the new round-trip test rather than a DB enum. **Verification**: new `pkg/repo/migration0002_test.go` adds 3 testcontainers-postgres tests — `TestMigration0002_NDAAcceptanceColumns` (information_schema column-shape assertion), `TestMigration0002_NDAAcceptanceCompositePK` (duplicate insert → 23505 unique violation; distinct hash and distinct user are non-violations), `TestMigration0002_AuditActionRoundTrip` (table-driven: every M2 action — `nda.accept`, `applicant.approve`, `applicant.reject`, `code.upload`, `code.upload_rejected`, `code.grant_orphaned`, `applicant.dm_sent`, `applicant.dm_failed`, `dm.circuit_opened`, `dm.circuit_closed`, `campaign.create`, `campaign.create_failed`, `campaign.generate_codes`, `campaign.generate_codes_failed` — round-trips its declared payload shape from schema.md, and system-emitted rows persist NULL `actor_user_id`). `truncateAll` in `pkg/repo/main_test.go` extended to include `nda_acceptance`. `go test ./...` PASS, `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS (binary applies migration 0002 on startup, exercising the up path against a fresh container). No new smoke probe added: phase 2 introduces no user-visible RPC behavior — the boot path itself is the migration's smoke coverage, per CLAUDE.md "pure refactors with no external surface change need no new smoke coverage but must keep the existing harness green".
3. `[x]` **Repository extensions (`pkg/repo`)** — `pkg/repo/nda.go` (insert + idempotent re-accept lookup); applicant CAS update for status/DM fields; code pool methods (`Reserve`, `FencedFinalize`, `Reclaim`, `BulkInsertCSV`, `BulkInsertGenerated`, `Pool stats`); audit JSONB writers for every new action. Integration test: the canonical fenced `UPDATE` from [`schema.md`](schema.md) §"Approve flow" returns 0 rows when `reservedBy`/`reservedAt` change between reserve and finalize (reclaim-and-steal scenario).

    **Outcome (2026-05-02)**: shipped. Four landings:
    - **`pkg/repo/nda.go`** + `nda_test.go` — `NDAAcceptanceStore` with `AcceptIdempotent` (INSERT … ON CONFLICT DO NOTHING + SELECT-on-miss; replay returns the original `accepted_at`), `Get`, and `LatestForApplicant` (powers the §5.3 `NdaReacceptRequired` derived-state check). 5 tests: replay returns the original row (`replayed=true`, original timestamp preserved), new hash on same `(user, playtest)` is a fresh acceptance, missing acceptance → `ErrNotFound`, `LatestForApplicant` returns the most recent hash, missing latest → `ErrNotFound`.
    - **Applicant CAS** in `applicant.go` — interface gains `ApproveCAS`, `RejectCAS`, `UpdateDMStatus`. CAS variants take a `repo.Querier` (new shared interface in `errors.go` satisfied by both `*pgxpool.Pool` and `pgx.Tx`) so the approve flow's fenced-finalize + applicant-update can run in the same caller-owned tx per `schema.md` §"Approve flow". `UpdateDMStatus` is latest-write-wins (manual Retry DM after a failure) and byte-truncates `lastDmError` to 500 bytes preserving valid UTF-8 codepoint boundaries via the new `truncateUTF8` helper. New exported enums `repo.ApplicantStatusPending/Approved/Rejected` for use in tests + future service code. `service.fakeApplicantStore` extended to satisfy the new interface (in-memory CAS + DM status update). Tests: `ApproveCAS` happy-path (PENDING → APPROVED with grant attribution), `ApproveCAS` double-approve loses CAS (errors.md row 11 — `ErrStatusCASMismatch`), `RejectCAS` happy-path + double-reject loses CAS, `UpdateDMStatus` truncates a 502-byte UTF-8 string to ≤500 bytes ending on a complete `é` codepoint.
    - **Code pool methods** in `code.go` — interface gains `Reserve`, `FencedFinalize`, `Reclaim`, plus alias methods `BulkInsertCSV` / `BulkInsertGenerated` (named for the call sites; both delegate to the existing CopyFrom-based `BulkInsert`). `Reserve` uses `UPDATE … WHERE id = (SELECT … FOR UPDATE SKIP LOCKED)` so concurrent reservers don't block each other and FIFO consumption is preserved (`ORDER BY created_at ASC`). Empty pool surfaces as `ErrPoolEmpty` (new sentinel) — service layer maps to the model-specific `ResourceExhausted` per errors.md rows 12-13. `FencedFinalize` runs the canonical SQL from `schema.md` §"Approve flow" verbatim, returning `RowsAffected` so the service can branch into `code.grant_orphaned` on 0 rows. `Reclaim` is pool-wide (no playtest filter) — releases RESERVED rows whose `reserved_at < NOW() - ttl`. Tests: `Reserve` happy-path + empty-pool sentinel; `FencedFinalize` happy-path; **`FencedFinalize` reclaim-and-steal returns 0 rows** (the explicit STATUS.md test — simulates reclaim-and-steal by user B; `userA, originalReservedAt` keyed UPDATE affects 0 rows and the row stays RESERVED for B); `Reclaim` releases stale reservations while leaving fresh ones untouched.
    - **Typed audit writers** in `auditlog_m2.go` — 14 package-level helpers (one per M2 action) on top of the existing `AuditLogStore.Append`. Each marshals the `schema.md` JSONB shape and persists via Append; system-emitted writers take no actor argument (encodes the attribution rule in the type system). Action constants `repo.ActionNDAAccept` … `repo.ActionCampaignGenerateCodesFailed` defined alongside. `AppendApplicantDMFailed` byte-truncates the `error` field to 500 bytes preserving UTF-8 codepoint boundaries (matches `applicant.last_dm_error` discipline + `dm-queue.md`). 10 tests cover: payload + actor for `nda.accept`; `applicant.approve` carries `grantedCodeId` and never leaks a raw code value (catches a future regression that adds a `value` field); `applicant.reject` reason round-trip; `code.upload` shape is exactly `{count, sha256, filename}` with no extras; `code.upload_rejected` is system-emitted (nil actor); `code.grant_orphaned` is system-emitted with timestamp round-tripping as RFC3339Nano; `applicant.dm_sent` is admin-attributed (Retry DM); `applicant.dm_failed` truncates a 502-byte UTF-8 error string to ≤500 bytes ending on a complete `é`; `dm.circuit_opened` / `dm.circuit_closed` are namespace-scoped (nil `playtest_id`) and system-emitted; the four `campaign.*` actions all persist with nil actor.

    **Verification**: `go test ./...` PASS (full module — repo package time grew from ~3s to ~4.3s with the new tests but no flakiness; `make smoke` exits 0 against a fresh container). `golangci-lint run` 0 issues — fixed two new `goconst` warnings by introducing `repo.ApplicantStatus*` constants and using the existing `applicantStatus*` constants in `pkg/service/playtest_test.go`. No new smoke-harness probe added: phase 3 introduces no user-visible RPC behavior — handler-layer phases 4–7 will each extend the smoke harness with their own probes.
4. `[x]` **`AcceptNDA` RPC + re-accept gating** — handler + idempotent natural-key replay per PRD §4.7 (second accept on same `(userId, playtestId, ndaVersionHash)` returns existing row, no error); confirm M1's `EditPlaytest` already writes `nda.edit` audits with full old/new text and recomputes `currentNdaVersionHash` (post-8.1 fix); surface `NdaReacceptRequired` derived state on `GetApplicantStatus` per PRD §5.3 client-detection rule. Player Svelte: NDA accept route. Smoke probe: accept → second accept (same row) → admin edits NDA text → derived state flips to require re-accept.

    **Outcome (2026-05-02)**: shipped. `pkg/service/applicant.go` grew `AcceptNDA`: `requireActor` → parse `playtest_id` UUID → `playtest.GetByID(namespace, id)` → DRAFT/soft-deleted → `NotFound` (mirrors `GetApplicantStatus`); `!NDARequired` → `InvalidArgument`; missing applicant → `FailedPrecondition "must signup before accepting nda"`; CLOSED + non-APPROVED → `NotFound` (CLOSED + APPROVED still allows re-accept per PRD §5.3 "previously GRANTED codes stay visible"). On the happy path, the handler calls `nda.AcceptIdempotent({userId, playtestId, hash=pt.CurrentNDAVersionHash})` — the live playtest hash, never client-supplied, makes the post-edit re-accept flow work without a request hash field. `replay==false` writes `applicant.nda_version_hash = pt.CurrentNDAVersionHash` (idempotent — only fires when the stored hash drifts) and emits a single `nda.accept` audit row (player-attributed, `actor = userID`); `replay==true` is a pure no-op write so a stuck client retrying never pollutes the audit timeline. `ndaAcceptanceToProto` returns `{userId, playtestId, ndaVersionHash, acceptedAt}` matching the `pkg/pb` shape from phase 1. **NdaReacceptRequired surfacing**: PRD §5.3 specifies client-side computation (`GetPlaytestForPlayer.currentNdaVersionHash != GetApplicantStatus.ndaVersionHash`); both fields already ship over the wire from M1 phases 6/7. New unit test `TestGetApplicantStatus_AndPlaytest_ExposeNdaHashesForClientComparison` asserts the server populates both so the client diff is computable. **EditPlaytest nda.edit audit (M1 spec backfill caught here per phase-4 confirm-or-fix instruction)**: `pkg/service/playtest.go` now captures the previous NDA text before the in-place mutation and, when the text actually changed, calls `repo.AppendNDAEdit(ns, playtestID, actorID, before, after)` after a successful `Update`. Per `schema.md` L42 this is the **only** audit row that intentionally persists the full free-text payload — every other typed writer redacts. New `pkg/repo/auditlog_m1.go` adds `ActionNDAEdit = "nda.edit"` plus the writer; `ActionPlaytestEdit`/`ActionPlaytestSoftDelete`/`ActionPlaytestStatusTransition` constants are also pinned now so future M1-completion phases can wire the rest of the M1 audit set without churning the audit-action namespace. Unit tests cover NDA text changed → exactly one `nda.edit` row with `before.ndaText`/`after.ndaText` correct; NDA text unchanged → zero rows even when other fields are edited (validates the conditional emission).

    **Repo + wiring**: `repo.ApplicantStore` gained `SetNDAVersionHash(ctx, applicantID, hash) → *Applicant` (single-column UPDATE, ErrNotFound on missing row). `internal/bootapp/bootapp.go` wires `repo.NewPgNDAAcceptanceStore(dbPool)` and `repo.NewPgAuditLogStore(dbPool)` via the new chainable `WithNDAStore` / `WithAuditLogStore` setters on `*PlaytesthubServiceServer`; the bare M1 constructor still works so unit tests that don't exercise NDA can stay one-line. AcceptNDA returns `Internal "nda store not wired"` when called without a wired store — explicit failure beats silent no-op for a wiring regression.

    **Player Svelte**: new `routes/Nda.svelte` route at `#/playtest/<slug>/nda`. Component fetches `GetPlaytestForPlayer` + `GetApplicantStatus` in parallel, applies the §5.3 client-side rule (no NDA needed *or* `applicant.ndaVersionHash === playtest.currentNdaVersionHash` → bounce to Pending), otherwise renders the NDA text in a scrollable region with an "I have read and agree" checkbox + Accept button gated on the checkbox. On accept it POSTs the AcceptNDA payload using `applicant.playtestId` (carried verbatim from the player-visible `Applicant` proto since `PlayerPlaytest` does not surface `id`). Signup now navigates to `/nda` instead of `/pending` on both 200 and 409 (idempotent re-signup); Pending fetches the player playtest alongside the applicant row and detects re-accept (`pt.ndaRequired && app.ndaVersionHash !== pt.currentNdaVersionHash`) → redirects to `/nda`. New API helpers in `lib/api.ts`: `fetchPlayerPlaytest`, `fetchApplicantStatusWithIds` (returns `Applicant & { playtestId }`), `acceptNda`. Vitest covers the new fetch shape on Pending (route-aware fetch stub keyed on URL) plus a "redirects to /nda on hash mismatch" case.

    **Verification**: `go test ./...` PASS, `golangci-lint run` 0 issues (after refactoring the new test's 5-tuple destructure into an `ndaTestRig` struct to satisfy `dogsled`), `make lint-proto` clean (no proto changes — phase 1's surface already declared the messages), `cd player && npm test` 62/62 PASS, `npm run build` clean. `scripts/smoke/boot.sh` PASS with two new probes: (a) native-gRPC AcceptNDA returns `Unauthenticated` (auth disabled, handler reached — proves it's no longer the embedded `Unimplemented` stub); (b) gateway `POST /v1/player/playtests/{id}:acceptNda` returns 401 (proves the grpc-gateway route + auth interceptor + handler are all wired). The richer accept→re-accept→edit→re-accept scenario from the STATUS line lives in `TestAcceptNDA_AfterEdit_NewRowAndAdvancedHash` (unit) + the future `pth flow golden-m2` (e2e); a DB-seeding smoke probe would duplicate the same coverage with more shell.
5. `[x]` **STEAM_KEYS code upload + `GetCodePool`** — `UploadCodes` enforces PRD §4.3: UTF-8, charset `[A-Za-z0-9._-]`, per-code length 1–128, file ≤10 MB, ≤50,000 codes, file-level + cross-row dedup, **whole-file reject on any violation** with offending line numbers in the response body. `pg_advisory_xact_lock(hashtext(playtestId::text))` for upload tx. `code.upload` and `code.upload_rejected` audit rows (no raw code values in payload — PRD §6 redaction). `GetCodePool` returns counts + raw code values (admin UI is exempt from log-redaction per PRD §5.7). Every [`errors.md`](errors.md) `UploadCodes` row asserted.

    **Outcome (2026-05-03)**: shipped. New `pkg/service/code.go` houses both handlers + the CSV parser. **Validation order** matches the PRD §4.3 cost gradient: file-size cap (`uploadCSVMaxBytes = 10 * 1024 * 1024`) is the first check so a junk multi-GB payload bottoms out without burning memory; UTF-8 validity is next (`utf8.Valid` over the byte slice); BOM stripped before line-splitting; lines split on `\n` with the trailing empty element dropped so a normal "K\nK\n" file isn't penalised. Per-line rules: trim whitespace → reject empty (`empty_line`); length 1–128 (`length_violation`); charset `^[A-Za-z0-9._\-]+$` (`charset_violation`); intra-file dedup (`duplicate_in_file`). Row-count cap (50,000) is checked against the parsed count after per-line validation so an oversized but otherwise valid file still surfaces `count_exceeded` clearly. **Whole-file reject** is honoured: a non-empty rejections list short-circuits before any DB roundtrip; the response carries `inserted=0` plus the per-line `UploadCodesRejection{line_number, reason, value}` slice — gRPC code is OK because the proto envelope is the structured channel. **Concurrency discipline** lives in `repo.PgCodeStore.UploadAtomic`: opens a tx, takes `pg_advisory_xact_lock(hashtext($playtestId::text))`, runs the dedup-against-DB SELECT inside the lock, and either copies the batch via `tx.CopyFrom` (clean) or rolls back without inserting (any DB-side dup → service maps duplicates back to CSV line numbers and returns the `duplicate_in_db` per-line rejections). The advisory lock + dedup-inside-lock combination matches PRD §4.3 footer ("dedup + insert run inside a single Postgres tx holding `pg_advisory_xact_lock(hashtext(playtestId::text))` — serializes uploads against other uploads for the same playtest"); approves continue to use row-level locks per §4.1 step 6, never touching the advisory lock. **Audit emission**: `code.upload` (admin-attributed) on success carries `{count, sha256(csvBytes), filename}` only — explicit unit test `TestUploadCodes_AuditPayloadCarriesNoRawValues` plants a canary and asserts the JSONB blob does not contain it (catches a future regression that adds a `value` field). `code.upload_rejected` (system-emitted, no actor) on every reject path; the `reason` field uses the `dominantReason` priority `charset > length > duplicate-in-file > empty` so the audit row carries the most actionable cause when a file mixes multiple violation types. STEAM_KEYS gating: AGS_CAMPAIGN playtests reject with `FailedPrecondition` ("use TopUpCodes/SyncFromAGS"); soft-deleted/missing playtests hide as `NotFound`; namespace mismatch is `PermissionDenied` (mirrors M1 admin RPCs).
    
    **`GetCodePool`** returns a `CodePoolStats` (total/unused/reserved/granted counted from the row list) plus the full `[]Code` including raw values per PRD §5.7 admin-exempt rule. Same visibility gates as UploadCodes (soft-delete → NotFound; both distribution models supported — admin needs to see AGS_CAMPAIGN pools too). `pkg/repo/code.go` gained `ListByPlaytest` (created_at ASC, id ASC — same FIFO order as `Reserve`).
    
    **Proto**: phase 1 already declared the wire shape, but the request was missing `filename` (audit row references it per schema.md L51). Added `string filename = 4` to `UploadCodesRequest` — additive, wire-compatible — and regenerated stubs (proto plugins match CI pins: protoc-gen-go v1.36.6, protoc-gen-go-grpc 1.5.1, protoc-gen-grpc-gateway/openapiv2 v2.27.1, protoc 21.12). `gateway/apidocs/api.swagger.json` regenerated cleanly.
    
    **Bootapp wiring**: `internal/bootapp/bootapp.go` now constructs `repo.NewPgCodeStore(dbPool)` and chains `WithCodeStore(...)` after `WithAuditLogStore`. Service-layer fakes in `pkg/service/code_test.go` cover the in-memory CodeStore surface (BulkInsert/CountByState/GetByID/ListByPlaytest/UploadAtomic + the M2 phase-3 approve-flow stubs satisfy the interface).
    
    **Tests** (16 new service tests, 4 new repo tests):
    - Service: happy path inserts + `code.upload` audit; redaction canary; file-size reject (no DB call); non-UTF-8 reject; row-count >50k reject; per-line charset/length/empty reject (whole-file with `dominantReason=charset_violation`); DB dedup with line-number mapping (line=2, reason=`duplicate_in_db`); BOM stripping; AGS_CAMPAIGN model reject; soft-deleted NotFound; missing actor → Unauthenticated; namespace mismatch → PermissionDenied; GetCodePool happy path with stats counters + raw values; GetCodePool soft-deleted NotFound; GetCodePool store-not-wired → Internal.
    - Repo (testcontainers-postgres): `UploadAtomic` happy path inserts under advisory lock; whole-file reject when any value collides with existing rows (returns dups list, inserts 0 rows, pool size unchanged); empty-values short-circuit; `ListByPlaytest` orders by created_at ASC and is scoped per playtest_id (a second playtest's rows do not bleed in).
    
    **Verification**: `go test ./...` PASS (full module — service tests run in 36ms, repo tests run in ~3s with testcontainers reuse), `golangci-lint run` 0 issues (added `distModelSteamKeys`/`distModelAGSCampaign` constants in `playtest.go` to silence the new `goconst` warning the third "STEAM_KEYS" literal would otherwise trip), `scripts/smoke/boot.sh` PASS with four new probes — native gRPC `UploadCodes` and `GetCodePool` both return Unauthenticated (handler reached, no longer Unimplemented), and the gateway routes `POST /v1/admin/.../codes:upload` and `GET /v1/admin/.../codes` both return 401 (proves grpc-gateway routing + auth interceptor chain). The remaining-Unimplemented probe was retargeted: phase 4's `AcceptNDA` Unimplemented check became phase 5's note that 7 M2 RPCs are still pending, with `GetGrantedCode` as the canary.
6. `[x]` **Approve / Reject + reclaim worker + leader lease** — `ApproveApplicant`: reserve → fenced finalize per [`schema.md`](schema.md); on 1 row affected, write `applicant.approve` audit and update applicant in same tx; on 0 rows, write `code.grant_orphaned` (system-attributed), roll applicant back to `PENDING`, return `Aborted "reservation expired, please retry"`. `RejectApplicant` (terminal; CAS on `Applicant.status`). `ListApplicants` cursor pagination (`createdAt DESC`, page 50) + status + DM-failed filter. `GetGrantedCode` (player; soft-deleted playtest → `NotFound`). Reclaim worker: DB-backed leader lease (TTL 30s, heartbeat 10s, env-tunable per PRD §5.9); 30s reclaim cadence; emits `{event:"reclaim_tick", released, leaseHolder}` INFO log line. Concurrency integration test: two goroutines race the same applicant; first wins with `applicant.approve`, second gets `FailedPrecondition "applicant already approved"`. Empty-pool returns the model-specific `ResourceExhausted` messages from [`errors.md`](errors.md). Every [`errors.md`](errors.md) row for these RPCs asserted in handler tests.

    **Outcome (2026-05-03)**: shipped. Five landings:

    - **`pkg/repo/txrunner.go`** — new `TxRunner` interface + `PgTxRunner` wrap `*pgxpool.Pool` so the service can chain `Reserve → FencedFinalize → ApproveCAS` inside one tx without leaking pgx into `pkg/service`. `InTx(ctx, fn)` rolls back on a non-nil error from `fn` and commits otherwise. Service tests substitute `fakeTxRunner{}` (runs `fn(nil)`); the canonical `TestPgTxRunner_RollsBackOnError` integration test in `pkg/repo` proves the rollback path keeps a `Reserve`'d row's pre-tx state intact.
    - **`pkg/repo/applicant.go`** — `ApplicantStore` gained `ListPaged(ctx, ApplicantPageQuery)`. SQL: `WHERE playtest_id = $1 [AND status=$2] [AND last_dm_status='failed'] [AND (created_at, id) < ($cur_ts, $cur_id)] ORDER BY created_at DESC, id DESC LIMIT $limit+1`. Cursor is base64(JSON `{c:created_at, i:id}`) — opaque to clients. Default limit 50, max 200, both per PRD §6 Pagination. New `ErrInvalidPageToken` sentinel surfaces malformed tokens as `InvalidArgument`. Test exports (`EncodeApplicantPageTokenForTest` / `DecodeApplicantPageTokenForTest`) let the in-memory `fakeApplicantStore` round-trip the same opaque format so a regression in the cursor encoding trips both unit and integration tests.
    - **`pkg/service/approve.go`** — four new RPCs:
      - `ApproveApplicant`: short-circuits on idempotent re-approve (returns existing row, no audit); REJECTED → `FailedPrecondition "applicant is rejected and cannot be re-approved"`; DRAFT → `FailedPrecondition "playtest is in draft; approve/reject requires OPEN status"`; CLOSED → `FailedPrecondition "playtest is closed; approve/reject is no longer allowed"`; soft-deleted → `NotFound`. Tx pipeline: `Reserve` → `FencedFinalize` → `ApproveCAS`. `ErrPoolEmpty` maps to model-specific `ResourceExhausted` (`"No codes remaining in pool. Upload more codes to continue approving."` for STEAM_KEYS, `"...Generate more codes..."` for AGS_CAMPAIGN — byte-exact errors.md rows 12/13). Fenced 0-row case writes `code.grant_orphaned` outside the rolled-back tx (best-effort) and returns `Aborted "reservation expired, please retry"` (errors.md row 10). On success emits `applicant.approve` audit. To stay under golangci-lint's `gocognit` cap, the handler is split into `resolveApproveContext` / `loadPlaytestForMutation` / `runApproveTx` / `recordOrphanedGrant` / `mapApproveTxError` helpers.
      - `RejectApplicant`: terminal CAS PENDING → REJECTED. Idempotent re-reject returns the existing row (no audit). REJECTED is never advanced to APPROVED; the inverse direction (rejecting an already-APPROVED applicant) returns `FailedPrecondition "applicant is approved and cannot be rejected"`. CAS race surfaces as `FailedPrecondition "applicant already approved"` (errors.md row 11). Optional `rejection_reason` round-trips into the `applicant.reject` audit row.
      - `ListApplicants`: cursor pagination + status filter (UNSPECIFIED → no filter) + dm_failed_filter. Default page 50, server-capped at 200. Soft-deleted playtest hides as `NotFound`. Bad page_token surfaces `InvalidArgument`.
      - `GetGrantedCode`: player-only RPC; soft-deleted playtest → `NotFound` regardless of applicant state (errors.md row 30); non-APPROVED applicant or missing `granted_code_id` → `NotFound`. Returns the raw value + the playtest's `distribution_model` so the player UI can render model-specific instructions.
      - `adminApplicantToProto` renders the full admin-visible field set (vs. `playerApplicantToProto`'s redacted subset); new `dmStatusStringToEnum` maps `applicant.last_dm_status` strings to the proto `DmStatus` enum.
    - **`internal/reclaim/`** — new package with `Worker` running the PRD §5.5 reclaim loop. Construction takes `Config{HolderID, LeaseTTL, HeartbeatInterval, ReclaimInterval, ReservationTTL}`; `New(cfg, leaderStore, Reclaimer, logger)` (the narrow `Reclaimer` interface = `Reclaim(ctx, ttl) (int64, error)` keeps the test fake one-method instead of the full `CodeStore` surface). Loop ticks at `HeartbeatInterval`; on every tick, leaders heartbeat then sweep, emitting `{event:"reclaim_tick", leaseHolder, released}` at INFO. Followers `TryAcquire` on each tick; expired leases steal cleanly via the existing `repo.LeaderStore.TryAcquire`. Lease released on context cancel. Errors during sweep / heartbeat are logged at WARN but never abort the loop — next tick retries. Tests use a `manualTicker` (`<-chan time.Time` driven by the test) to drive deterministic firings without sleeps.
    - **`main.go`** — starts the reclaim worker as a background goroutine after `bootapp.New` returns. `holderIDFromEnv()` reads `POD_NAME`, then `HOSTNAME`, then `os.Hostname()`, with `"playtesthub"` as the final fallback so the elected leader is always identifiable in logs even on dev workstations. `bootapp.go` wires the new `repo.PgTxRunner` into the service via `WithTxRunner(...)`.

    **Concurrency integration test**: `pkg/repo/approve_concurrency_test.go::TestApprove_ConcurrentRace_FirstWinsSecondCASMismatches` spins two goroutines that each run the full `Reserve → FencedFinalize → ApproveCAS` pipeline against the same PENDING applicant on a real Postgres (testcontainers). Asserts: exactly 1 winner (no error) + 1 loser (`ErrStatusCASMismatch`); final applicant state is APPROVED; pool ends with exactly 1 GRANTED code (the loser's reservation rolled back, not stranded). `TestPgTxRunner_RollsBackOnError` confirms the tx wrapper unwinds a `Reserve`'d row when `fn` returns a sentinel error.

    **Tests**: 25 service unit tests (`approve_test.go`) — happy path, every errors.md row asserted (`Aborted "reservation expired, please retry"`, `FailedPrecondition "applicant already approved"`, `FailedPrecondition "applicant is rejected and cannot be re-approved"`, `ResourceExhausted` for both STEAM_KEYS + AGS_CAMPAIGN, `FailedPrecondition` for DRAFT + CLOSED), + idempotent re-approve / re-reject, + ListApplicants pagination/status/dm-failed filter/bad-token, + GetGrantedCode soft-delete/not-approved/unauth. 4 reclaim-worker unit tests (`internal/reclaim/reclaim_test.go`) — leader acquires + sweeps + emits log line; follower skips Reclaim; cancel releases lease; sweep error logs WARN but loop continues. 4 repo integration tests — concurrency race + tx-rollback + 2 `ApplicantListPaged` cursor/filter tests (testcontainers-postgres).

    **Verification**: `go test ./...` PASS (full module — `pkg/repo` time grew ~3.0s → 3.2s with the concurrency + paged-list tests; reclaim worker tests run in 0.094s with deterministic ticker), `golangci-lint run` 0 issues (after splitting `ApproveApplicant` into helpers and pinning `dmStatusSent` / `dmStatusFailed` constants), `make lint-proto` clean (no proto changes — phase 1 surface already declared the messages), `scripts/smoke/boot.sh` PASS with 9 new probes:
    - native gRPC: `ApproveApplicant` / `RejectApplicant` / `ListApplicants` / `GetGrantedCode` all return Unauthenticated (handler reached, not Unimplemented);
    - gateway HTTP: same four routes return 401 Unauthenticated (`POST /v1/admin/.../applicants/{id}:approve`, `:reject`, `GET /v1/admin/.../playtests/{id}/applicants`, `GET /v1/player/playtests/{id}/grantedCode`);
    - the previously-pending probe was retargeted from `GetGrantedCode` to `RetryDM` (the canary for the still-pending phase 7 RPCs);
    - reclaim worker: a probe waits up to 12s for the first `{event:"reclaim_tick"}` log line; smoke env tightens `LEADER_HEARTBEAT_SECONDS=1`, `LEADER_LEASE_TTL_SECONDS=5`, `RECLAIM_INTERVAL_SECONDS=1`, `RESERVATION_TTL_SECONDS=5` so the worker fires inside the smoke timeout instead of the PRD §5.9 production defaults.
7. `[x]` **DM queue (`pkg/dmqueue`) + `RetryDM`** — bounded in-memory FIFO (default 10k via env), worker drain rate (default 5/sec, env `DM_DRAIN_RATE_PER_SEC`), restart sweep with idempotency guard (re-marks only `lastDmStatus IS NULL OR 'pending'`; preserves prior `failed` reason), circuit breaker (50 failures / 60s → 5 min pause; auto-resume), all four `lastDmError` reasons (`dm_queue_overflow`, `lost_on_restart`, `dm_circuit_open`, generic Discord error). `RetryDM` admin RPC (no cooldown — double-click sends two DMs per PRD §5.4). Audit rows: `applicant.dm_sent` (manual retry only — admin-attributed), `applicant.dm_failed`, `dm.circuit_opened/closed` (system-attributed). Truncate `lastDmError` to 500 bytes preserving UTF-8 boundary per [`dm-queue.md`](dm-queue.md). Smoke covers: approve enqueues → drains, kill+restart marks `lost_on_restart`, RetryDM flips `failed` → `sent`. **Cut-if-behind**: circuit breaker may slip to M3 (already a candidate per M3 row below).

    **Outcome (2026-05-03)**: shipped. Six landings:

    - **`pkg/dmqueue`** — new package with `Queue` (bounded `chan Job`, default 10k via `DM_QUEUE_MAX_DEPTH`), `Sender` interface (`SendDM(ctx, discordUserID, message)`), `ApplicantUpdater` (the slice of `repo.ApplicantStore` the worker needs — narrowed to `UpdateDMStatus` + `ListLostDMOnRestart` so unit-test fakes stay tiny). `Enqueue` is non-blocking: a full buffer synchronously writes `lastDmStatus='failed'` with `lastDmError='dm_queue_overflow'` + an `applicant.dm_failed` audit row, then returns `ErrQueueFull`. `Run` drains at `DrainRatePerSec` via a fixed throttle (`time.Second / rate`); `process` per-job pipeline: circuit-open check → `Sender.SendDM` → `markSent` (clears the trailing-failure window) or `markFailed` + `recordFailure`. **Circuit breaker**: 50 failures within 60s trip the breaker for 5 minutes (auto-resume); subsequent jobs while tripped are short-circuited to `markFailed(reason=dm_circuit_open)` without invoking `Sender`. `dm.circuit_opened` (with `recentFailureCount`) and `dm.circuit_closed` audit rows bracket the open period — both system-emitted, namespace-scoped (no `playtest_id`) per `schema.md`. **Sweep** is the boot-time idempotency guard (`docs/dm-queue.md` "Restart behavior"): one shot at startup, scans every APPROVED applicant in the namespace whose `last_dm_status IS NULL OR 'pending'`, marks them `failed` with `lost_on_restart` and writes an `applicant.dm_failed` audit row each. Already-`failed` rows are skipped so the original reason (`dm_queue_overflow`, `dm_circuit_open`, generic Discord error) survives the restart. **Manual-only audit rule** lives in `markSent`: only `Manual=true` jobs (i.e. RetryDM-driven sends) emit `applicant.dm_sent` on success — the auto-send from `ApproveApplicant` deliberately writes no audit row, matching PRD §5.4 ("`applicant.dm_sent` (manual retry only)").

    - **`pkg/repo/applicant.go`** — `ApplicantStore.ListLostDMOnRestart(ctx, namespace)` SQL: `SELECT … FROM applicant a JOIN playtest p ON p.id = a.playtest_id WHERE p.namespace = $1 AND p.deleted_at IS NULL AND a.status = 'APPROVED' AND (a.last_dm_status IS NULL OR a.last_dm_status = 'pending')`. The JOIN on namespace + deleted_at scopes the sweep to one AGS instance and skips soft-deleted playtests (whose applicants are no longer admin-visible anyway). New `applicantColumnsPrefixed("a")` helper renders the column list aliased so the JOIN can disambiguate `id`/`playtest_id`; the existing single-table queries keep using the unprefixed `applicantColumns` const. Interface tagged `//nolint:interfacebloat` — the surface is now 11 methods (one over the cap), but splitting just to satisfy the rule would scatter related applicant-row methods across multiple interfaces and force every caller to compose them.

    - **`pkg/service/retry_dm.go`** — `RetryDM` handler. `DMEnqueuer` interface (`Enqueue(ctx, dmqueue.Job) error`) lets unit tests inject a fake while production wires `*dmqueue.Queue` (which already satisfies it). New `WithDMQueue(...)` chainable setter. Validation: `requireActor` → namespace check → `dm queue not wired` Internal guard → applicant UUID parse → `applicant.GetByID` → `Status != APPROVED` → `FailedPrecondition "retry dm requires approved applicant"`; `LastDMStatus != failed` → `FailedPrecondition "retry dm requires last dm status=failed"` (the `dm-queue.md` Retry-DM gate); playtest visibility (soft-deleted → NotFound). Then `Enqueue(buildDMJob(applicant, playtest, manual=true))`. PRD §5.4's "no cooldown — double-click sends two DMs" is a natural consequence of the gate: between two clicks the applicant still shows `failed` (worker hasn't drained the first job yet) so both clicks pass the gate and enqueue. On `ErrQueueFull` the handler re-fetches the applicant so the response carries the synchronous `failed`+`overflow` mark instead of the stale row. **`buildDMJob`** uses `applicant.discord_handle` as the recipient: for users whose signup hit the Discord lookup-failure path (PRD §10 M1) it is the raw snowflake and DM delivery works; for users whose lookup succeeded it is the human display name and DM delivery fails with a generic Discord error which surfaces in the admin "DM failed" filter — closing the snowflake-storage gap is a future-migration task that does not change the queue contract.

    - **`pkg/service/approve.go`** — `ApproveApplicant` enqueues an auto-send DM (`Manual=false`) immediately after the success-path audit. The enqueue is fire-and-forget — its error (overflow or otherwise) is intentionally dropped so the approve RPC stays non-blocking on DM behaviour, matching `dm-queue.md` ("Approval RPCs return immediately; DM delivery is asynchronous"). Overflow still surfaces correctly because `dmqueue.Enqueue` writes the failed-status row + audit before returning; the admin sees the failed DM in the next list refresh.

    - **`internal/bootapp/bootapp.go`** + **`main.go`** — `bootapp.New` constructs the `dmqueue.Queue` alongside the playtesthub service and surfaces it on `Server.DMQueue` so `main.go` can run the sweep + worker. Production `Sender` is a no-op closure (`noopDMSender`) — every drain succeeds and marks `last_dm_status='sent'`, exercising the queue + worker + audit pipeline end-to-end without making outbound Discord calls. The real Discord DM client lands in M3 (PRD §M3 — "Discord DM notification on approval"); swapping it in is a one-line change at the construction site. `main.go` runs `Sweep(ctx)` once before starting the worker so any APPROVED row that was awaiting a DM at last shutdown surfaces as `lost_on_restart` before new approves enqueue. Sweep failure logs at WARN but does not block boot. The worker emits `{event:"dm_queue_started", maxDepth, drainRatePerSec}` at INFO so smoke can observe it; restart sweep emits `{event:"dm_restart_sweep", affected}`.

    - **`pkg/config/config.go`** — new `DMQueueMaxDepth` field (env `DM_QUEUE_MAX_DEPTH`, default 10000). `DMDrainRatePerSec` was already wired in M1 phase 2 with default 5; phase 7 connects it to the queue.

    **Tests**: 7 `pkg/dmqueue` unit tests — drain happy-path with no `applicant.dm_sent` audit on auto-send (the manual-only rule), drain with `applicant.dm_sent` audit on manual send, overflow synchronously marks `dm_queue_overflow` + writes `applicant.dm_failed`, send-failure persists the error string into `last_dm_error` and audit, circuit breaker trips at 50 failures + writes exactly one `dm.circuit_opened` row + short-circuits subsequent jobs without invoking `Sender` + auto-resumes after `CircuitOpenDuration` and writes exactly one `dm.circuit_closed` row, sweep marks every `(NULL, pending)`-status APPROVED row failed with `lost_on_restart` + an audit row each. 6 `pkg/service` unit tests for `RetryDM` — happy path enqueues a `Manual=true` job carrying applicant ids + playtest title + discord_handle as recipient; double-click enqueues twice (no cooldown); pending applicant → `FailedPrecondition`; sent-status applicant → `FailedPrecondition`; missing actor → `Unauthenticated`; `dm queue not wired` → `Internal`. 1 service test for `ApproveApplicant` confirms the auto-send enqueue carries `Manual=false`.

    **Verification**: `go test ./...` PASS (full module — `pkg/repo` time grew ~3.0s → ~3.0s; reclaim + dmqueue tests deterministic with manual clock + no real sleeps), `golangci-lint run` 0 issues (after splitting `Queue.Sweep` above its private helpers for `funcorder`, replacing `"failed"`/`"sent"` literals with `dmStatusFailed`/`dmStatusSent` constants for `goconst`, and tagging `ApplicantStore` with `//nolint:interfacebloat` for the 11-method case), `make lint-proto` clean (no proto changes — phase 1 surface already declared the messages), `scripts/smoke/boot.sh` PASS with three new probes:
    - native gRPC `RetryDM` returns Unauthenticated (handler reached, not Unimplemented);
    - gateway HTTP `POST /v1/admin/.../applicants/{id}:retryDm` returns 401 Unauthenticated;
    - boot log file contains `{event:"dm_queue_started"}` (proves `bootapp.New` constructed the queue, `main.go` ran `Sweep`, and the worker goroutine started). The previously-pending probe was retargeted from `RetryDM` to `TopUpCodes` (the new canary for the still-pending phase 9 RPCs). The "approve enqueues → drains" + "kill+restart marks lost_on_restart" + "RetryDM flips failed → sent" scenarios from the STATUS line live in the unit suite + the future `pth flow golden-m2` (phase 12); a DB-seeding smoke probe would duplicate the same coverage with more shell, matching the precedent set in phases 4 and 6.
8. `[x]` **AGS Platform client (`pkg/ags`) + AGS_CAMPAIGN auto-provision (sub-caps 1–4)** — wrap accelbyte-go-sdk Platform API: Item create (ENTITLEMENT type, derived from title/description), Campaign create, code generate (`AGS_CODE_BATCH_SIZE` default 1000), code fetch with pagination, Item/Campaign delete cleanup. Retry policy: 30s timeout + 3 retries with exponential backoff on HTTP 5xx/timeout; HTTP 4xx fail immediately (429 → `ResourceExhausted`); 5xx exhausted → `Unavailable` per [`errors.md`](errors.md). **Initial-create exception**: 300s timeout, no retries, all-or-nothing. Drop M1's `CreatePlaytest(distributionModel=AGS_CAMPAIGN) → Unimplemented` stub; wire Item → Campaign → generate → fetch inside one DB tx with the cleanup matrix from [`ags-failure-modes.md`](ags-failure-modes.md) on partial failure. HTTP 2xx with `count < requested` and no error → partial fulfillment, commit + WARN. Audits: `campaign.create`, `campaign.create_failed`, `campaign.generate_codes`, `campaign.generate_codes_failed`. Smoke against the maintainer's ISC namespace gated like the existing `PTH_E2E_*` block in `scripts/smoke/`.

    **Outcome (2026-05-03)**: shipped. Six landings:

    - **`pkg/ags`** — new package with `Client` interface (`CreateItem` / `CreateCampaign` / `CreateCodes` / `FetchCodes` / `DeleteItem` / `DeleteCampaign`), the `ItemSpec` / `CampaignSpec` / `CodeBatchResult` value types, sentinel errors (`ErrRateLimited` → `ResourceExhausted`; `ErrUnavailable` → `Unavailable`; typed `*ClientError` for non-429 4xx). `RetryPolicy.Run` is the shared retry loop: per-attempt `context.WithTimeout`, exponential backoff (cancel-aware via `time.After`), classification via `HTTPStatusCarrier` interface so SDK and test errors share one path. `DefaultRetryPolicy` = 30s/4 attempts/250ms→5s; `WithoutRetries` = 300s/1 attempt for the initial-create exception. `MemClient` is the in-memory implementation used by unit tests, the e2e harness, and bootapp default — every method has an `XxxErr []error` slot for failure injection (head consumed per call), `PartialFulfillment[campaignID]int` exercises the §4.6 commit-and-warn path, `AllItems()` / `AllCampaigns()` / `HasItem()` / `HasCampaign()` are test-introspection helpers.

    - **`pkg/repo` extensions** — `PlaytestStore.CreateTx(ctx, q Querier, p)` and `CodeStore.BulkInsertGeneratedTx(ctx, q Querier, playtestID, values)` are the tx-bound variants. `Create` collapses to `CreateTx(s.pool, ...)` so the existing STEAM_KEYS path is unaffected. `BulkInsertGeneratedTx` requires a real `pgx.Tx` (not the pool) because `pgx.Tx.CopyFrom` is the underlying call — the type assertion makes that boundary explicit. `CodeStore` is now 12 methods; tagged `//nolint:interfacebloat` because the natural splits (CSV / generated / list / approve / tx variants) all share the `(playtestID, ...)` key and force every caller to compose stores.

    - **`pkg/service/ags_campaign.go`** — `createAGSCampaignPlaytest` is the AGS_CAMPAIGN branch of `CreatePlaytest`. Order: (1) wire-check (ags client + tx runner + code store all required → `Internal` if any nil), (2) wrap `ctx` in a 300s `context.WithTimeout` for the entire AGS sequence per PRD §4.6 exception, (3) `agsCreateItem` → on failure write `campaign.create_failed{cleanupAttempted:false}` + `mapAGSError`, (4) `agsCreateCampaign` → on failure delete the orphaned item (cleanup matrix step 1, log WARN if delete fails) + audit + return, (5) `agsCreateCodesBatched` walks `agsCodeBatchSize` chunks via `WithoutRetries.Run` so each chunk gets a fresh attempt timeout — a partial-fulfillment batch breaks the loop early and the caller commits what was returned, (6) one `txRunner.InTx` runs `playtest.CreateTx` then `code.BulkInsertGeneratedTx` so the playtest row + every code row commit atomically; tx rollback runs the cleanup matrix step 2 (delete campaign + item, log WARN), (7) success-path audits `campaign.create` + `campaign.generate_codes`. Partial fulfillment surfaces as a WARN log (`"Requested {requested} codes, received {actual}. You may need to top up."`) and commits the smaller pool. The pre-INSERT `validateAGSCampaignRequest` enforces the §4.6 / errors.md row 27 1–50,000 bound and rejects missing `initial_code_quantity` with `InvalidArgument`. `mapAGSError` is the docs/errors.md byte-exact mapper: `ErrRateLimited` → `ResourceExhausted "upstream AGS rate limited (HTTP 429)"`, `ErrUnavailable` → `Unavailable "upstream AGS unavailable (retries exhausted)"`, `*ClientError` → `Internal "ags Op: client error N: msg"`.

    - **`pkg/service/playtest.go`** — service struct grew `agsClient ags.Client`, `agsCodeBatchSize int`, `logger *slog.Logger`. New chainable setters `WithAGSClient`, `WithAGSCodeBatchSize`, `WithLogger`. `CreatePlaytest` AGS_CAMPAIGN selector validates `initial_code_quantity` first, then constructs the `*repo.Playtest` with `DistributionModel=AGS_CAMPAIGN` + `InitialCodeQuantity=&qty`, then dispatches to `createAGSCampaignPlaytest`. STEAM_KEYS path keeps its old short-circuit on `initial_code_quantity` set (errors.md row 20). The previous `Unimplemented "distribution_model=AGS_CAMPAIGN lands in M2"` short-circuit is removed.

    - **`internal/bootapp/bootapp.go`** — `buildPlaytesthubServer` constructs `ags.NewMemClient()` and chains `WithAGSClient`, `WithAGSCodeBatchSize(cfg.AGSCodeBatchSize)`, `WithLogger(logger)`. Production wires `MemClient` so the boot path + smoke harness exercise the full AGS_CAMPAIGN code path without an outbound AGS round-trip — the SDK-backed adapter lands in **phase 8.1** once a live ISC namespace is available for sub-cap go/no-go validation per [`docs/ags-failure-modes.md`](ags-failure-modes.md) M2 sub-cap matrix. Swapping in the SDK adapter is a one-line change at the construction site, identical to the M3 Discord DM client transition pattern from phase 7.

    - **`pkg/config/config.go`** — `AGSCodeBatchSize` was already present (env `AGS_CODE_BATCH_SIZE`, default 1000) from M1 phase 5; phase 8 wires it into the new `WithAGSCodeBatchSize` setter.

    **Tests**: 16 `pkg/ags` unit tests — `Run` happy/4xx/429/5xx-retry-then-success/5xx-exhausted/timeout-retry-then-unavailable/ctx-pre-cancel/`WithoutRetries`-singleshot/non-HTTP-transient-then-unavailable + policy-shape assertions; MemClient happy-path/partial-fulfillment/injected-failures/unknown-item/idempotent-delete. 9 `pkg/service` unit tests for the AGS_CAMPAIGN branch — happy path generates codes + audits `campaign.create`+`campaign.generate_codes`, batched generation walks `agsCodeBatchSize=2` for `quantity=5` (2+2+1) and inserts all 5 rows, partial-fulfillment commits the smaller pool via the custom `partialFulfillClient`, item-create-fails surfaces `Unavailable` + writes `campaign.create_failed`+0 successes, campaign-create-fails deletes the orphaned item, codes-fails deletes both item and campaign + writes `campaign.generate_codes_failed`, 429 maps to `ResourceExhausted`, qty-out-of-bounds (0/-1/50001) returns `InvalidArgument`, missing AGS client wiring returns `Internal`, plus a `mapAGSError` table-test for each error class. The previous M1 `TestCreatePlaytest_AGS_CAMPAIGN_ReturnsUnimplemented` is replaced by `TestCreatePlaytest_AGS_CAMPAIGN_MissingInitialQty_InvalidArgument`. `fakePlaytestStore.CreateTx` and `fakeCodeStore.BulkInsertGeneratedTx` were added as the tx-aware fakes (delegating to the existing pool-direct fakes — the fake `txRunner` ignores its Querier arg per phase 6 precedent). New `fakeAuditLogStore.countAction` helper supports the audit-row-count assertions.

    **Verification**: `go test ./...` PASS (full module — `pkg/repo` time held at ~2.8s; pkg/ags retry test takes 0.165s due to the 50ms × 3 = 150ms timeout-exhaustion case being the slowest), `golangci-lint run` 0 issues (after tagging `CodeStore` `//nolint:interfacebloat`, replacing the `if cap > batchSize` with `min()`, dropping an ineffectual `sleep := p.Sleep` assignment, renaming the `cap` variable in MemClient to dodge the `predeclared` linter, gofmt-aligning `retry_test.go`, swapping `400` for `http.StatusBadRequest`, and reordering helper methods to satisfy `funcorder`), `make lint-proto` clean (no proto changes — phase 1 surface already declared the messages), `scripts/smoke/boot.sh` PASS with one new probe and one update:
    - new probe: `POST /v1/admin/.../playtests` with `distribution_model=AGS_CAMPAIGN` + `initial_code_quantity=10` returns 401 over the gateway (proves the new branch dispatches and the request shape parses; a regression that re-enabled the M1 Unimplemented stub would surface 501 here);
    - the existing "still Unimplemented" canary stays on `TopUpCodes` (the new canary for the still-pending phase 9 RPCs); the comment was updated to reflect that phases 4–8 have shipped.

    A live-AGS smoke probe gated like the `PTH_E2E_*` block was deferred to phase 8.1 because it requires the SDK-backed adapter; the maintainer's ISC namespace can be validated against the existing AGS_CAMPAIGN unit + integration coverage until then.
9. `[x]` **`TopUpCodes` + `SyncFromAGS` (sub-caps 6/7)** — `TopUpCodes`: not idempotent, generate-only, no DB tx (each batch inserted independently), holds `pg_advisory_xact_lock(hashtext(playtestId::text))` (same discipline as CSV upload). `SyncFromAGS`: fetch-only recovery, idempotent by `UNIQUE(playtestId, value)`. **Cut-if-behind candidate** per the §M2 cut tracker below: ship initial-generate only, hide top-up/sync in admin UI, defer the RPCs post-v1.0. Default position: ship.

    **Outcome (2026-05-04)**: shipped. Three landings:

    - **`pkg/repo/code.go`** — new `CodeStore.InsertGeneratedAtomic(ctx, playtestID, values) -> (insertedCount, err)`. Same advisory-lock-per-playtest discipline as `UploadAtomic` (BeginTx → `pg_advisory_xact_lock(hashtext($playtestId::text))` → dedup-against-DB SELECT → `tx.CopyFrom` over the diff → Commit), but unlike `UploadAtomic`'s whole-file-reject behaviour it silently skips already-present `(playtest_id, value)` rows and returns the count of fresh inserts. Serves both Top-up (defensive against an AGS replay returning a known value) and Sync (idempotent on `UNIQUE (playtest_id, value)`). `CodeStore` is now 13 methods; tagged `//nolint:interfacebloat` for the same reason as phase 8 — splitting just to satisfy the rule would scatter related accessors across multiple stores.

    - **`pkg/service/topup_codes.go`** — `TopUpCodes` and `SyncFromAGS` handlers + helpers. **TopUpCodes** validates `quantity ∈ [1, 50_000]` (errors.md row 27 — same bounds as `initial_code_quantity` per PRD §4.6), `requireActor` → `checkNamespace` → `requireAGSCodePathWired` (Internal on missing AGS client / code store) → `loadAGSCampaignPlaytest` (NotFound on missing/soft-deleted; FailedPrecondition on STEAM_KEYS or missing `ags_campaign_id`) → `generateAndPersistTopUp`. The generation loop walks `agsCodeBatchSize` chunks via `ags.DefaultRetryPolicy()` (30s/4 attempts/exponential backoff per docs/ags-failure-modes.md "Retry policy" — distinct from `CreatePlaytest`'s 300s/no-retry initial-create exception); each AGS round-trip's response feeds a separate `code.InsertGeneratedAtomic` call, so a mid-loop AGS failure preserves prior batches (PRD §4.6 step 4: "no DB tx — each batch inserted independently"). Audits: one `campaign.generate_codes` row per call carrying the total `added` (system-emitted; same writer as initial-create), `campaign.generate_codes_failed` on AGS failure. Partial fulfillment surfaces as a WARN log (`warnPartialFulfillment` reused from phase 8) and commits the smaller pool. **SyncFromAGS** mirrors the visibility gates, calls `agsClient.FetchCodes` under the same retry policy, and persists the dedup'd diff via `InsertGeneratedAtomic` in one tx — a re-sync after a successful sync inserts zero rows and emits no audit row (the `if added > 0` audit gate). Both handlers map AGS sentinels via the existing `mapAGSError`: `ErrRateLimited` → `ResourceExhausted` (errors.md row 23), `ErrUnavailable` → `Unavailable` (row 24), `*ClientError` → `Internal`. Pool stats come from `ListByPlaytest` post-call so the response carries the live counters.

    - **`internal/bootapp/bootapp.go`** — no new wiring required. Phase 8 already wires `WithAGSClient` + `WithCodeStore` + `WithAuditLogStore` on the production server; phase 9's handlers compose the same trio.

    **Tests**: 22 new service unit tests (`pkg/service/topup_codes_test.go`) — TopUpCodes happy path with audit assertion, `agsCodeBatchSize=2` walks 2+2+1 batches and calls `InsertGeneratedAtomic` 3 times, partial fulfillment via `partialFulfillClient` commits the smaller pool, mid-loop AGS 5xx (with 4 errors queued to exhaust retry budget) preserves batch 1 and surfaces `Unavailable` + `campaign.generate_codes_failed` audit, 429 → `ResourceExhausted`, STEAM_KEYS → `FailedPrecondition`, soft-deleted → `NotFound`, qty 0 / -1 / 50_001 → `InvalidArgument`, missing actor → `Unauthenticated`, namespace mismatch → `PermissionDenied`, missing AGS client → `Internal`; SyncFromAGS happy path adds 3 codes + audits, idempotent re-sync inserts 0 + no second audit, partial-overlap (2-of-5 already present) adds 3 fresh, AGS fetch 5xx → `Unavailable` + `campaign.generate_codes_failed` audit, STEAM_KEYS → `FailedPrecondition`, soft-deleted → `NotFound`, missing `ags_campaign_id` → `FailedPrecondition`, missing actor → `Unauthenticated`. The new `agsCampaignPlaytest` helper drives the `ags.MemClient` to issue real Item + Campaign ids so the playtest fixture is self-consistent with the AGS-side state every test asserts on. 4 new repo integration tests (`pkg/repo/code_test.go`, testcontainers-postgres) — `InsertGeneratedAtomic` happy path inserts under advisory lock; dedup-and-add-only-missing skips OLD-1/OLD-2 and inserts NEW-A/NEW-B leaving the pool at 4; all-dups returns 0 with no error (no-op idempotent re-Sync); empty values short-circuits before the BeginTx.

    **Verification**: `go test ./...` PASS (full module — `pkg/service` time grew ~0.5s → ~3.5s due to the mid-loop-failure test's exponential backoff sleeps (250ms+500ms+1s ≈ 1.75s per failed batch); deterministic — no flaky timing); `golangci-lint run` 0 issues; `make lint-proto` clean (no proto changes — phase 1's surface already declared the messages); `scripts/smoke/boot.sh` PASS with four new probes — native gRPC `TopUpCodes` and `SyncFromAGS` both surface `Unauthenticated` (handler reached, no longer `Unimplemented`); gateway `POST /v1/admin/.../codes:topUp` and `POST /v1/admin/.../codes:syncFromAgs` both return 401 (proves grpc-gateway routing + auth interceptor + handler chain wired). The previously-pending `TopUpCodes` Unimplemented canary was retired — every M2 RPC is now wired; the "remaining Unimplemented" log line was rewritten to a "no remaining stubs" assertion since admin UI / player Svelte / CLI / e2e are the only outstanding M2 phases (10–14) and they do not add server-side handlers.
10. `[x]` **Admin UI — applicants page (§5.7 page 2) + key pool page (§5.7 page 4)** — Extend App UI under `admin/`. **Applicants page**: list with status + DM-failed filters, per-row Approve/Reject confirms, Retry-DM button, low-pool banner (≤10% remaining), `RejectApplicant` confirm dialog with optional reason text field. **Key pool page**: STEAM_KEYS CSV uploader (per-line validation feedback on reject), AGS_CAMPAIGN Generate-more / Sync-from-AGS / Top-up actions (last two hidden if §9 cut applies), raw code values displayed (admin exempt from log-redaction per PRD §5.7), low-pool banner. Re-run `npm run codegen` against fresh OpenAPI; commit drift. **Cut-if-behind**: rejection-reason text field (`Applicant.rejectionReason` stays in schema; only the input field is the cut candidate).

    **Outcome (2026-05-04)**: shipped. Five landings:

    - **Codegen refresh** — `admin/swaggers/playtesthub.json` reseeded from the local proto-derived `gateway/apidocs/api.swagger.json` (the deployed-namespace URL referenced in `swaggers.json` is unreachable from a clean checkout); `npm run cg:clean-and-generate` rebuilt the typed clients + react-query hooks under `admin/src/playtesthubapi/` so the 10 M2 endpoints (`GetApplicants_ByPlaytestId`, `CreateApplicant_ByApplicantId{Approve,Reject,RetryDm}`, `GetCodes_ByPlaytestId`, `CreateCodes{Upload,TopUp,SyncFromAg}_ByPlaytestId`, plus the existing M1 hooks) are typed end-to-end. Both `swaggers/` and `src/playtesthubapi/` remain gitignored per the existing CI policy (engineering.md §5 / `.gitignore` lines 251-252) — the smoke harness regenerates them on every run.

    - **`admin/src/federated-element.tsx` route + nav extensions** — two new routes (`/:playtestId/applicants`, `/:playtestId/codes`) registered in `<Routes>`; the playtests-list row Actions column gained "Applicants" and "Codes" buttons next to "Edit". `PlaytestCreatePage`'s AGS_CAMPAIGN radio is no longer `disabled` — phase 8 made the backend handler real, so the M2 tooltip + `disabled` prop were dropped (the existing `initial_code_quantity` numeric input remains conditional on the AGS_CAMPAIGN selection).

    - **`ApplicantsPage`** — `usePlaytesthubServiceAdminApi_GetApplicants_ByPlaytestId` (with status + dm_failed_filter query params) + `GetCodes_ByPlaytestId` (for the low-pool banner) drive a single `<Table>` with columns Discord / Platforms / Status / DM / Created / Actions. Status filter is a `<Select>` mirroring the `V1ApplicantStatus` enum (UNSPECIFIED ↔ "All statuses"); DM-failed filter is a `<Checkbox>`. Per-row Approve uses `<Popconfirm>` ("A code will be reserved and granted from the pool."), disabled when status≠PENDING. Reject opens an antd `<Modal>` with a `<TextArea>` for the optional `rejection_reason` (capped at 500 chars; placeholder reminds operators it is admin-side only). Retry-DM button only renders when `status=APPROVED && lastDmStatus=DM_STATUS_FAILED` per PRD §5.4 + dm-queue.md gate. `LowPoolBanner` (extracted helper, shared with `CodePoolPage`) shows a yellow `<Alert>` when `unused/total ≤ 10%` of the pool. All four mutations (`approve`, `reject`, `retry-dm`, plus the implicit invalidations) call `queryClient.invalidateQueries` on both `Applicants_ByPlaytestId` and `Codes_ByPlaytestId` so the table + banner refresh together after every action; error envelopes surface via the same `err.response?.data?.errorMessage` pattern the M1 mutations already use.

    - **`CodePoolPage`** — `GetPlaytest_ByPlaytestId` + `GetCodes_ByPlaytestId` drive the page; the playtest's `distributionModel` selects between two mutually-exclusive sections. STEAM_KEYS path shows an antd `<Upload>` with `beforeUpload={() => false}` (blocks autosend) feeding a `FileReader` that hydrates `csvText`+`csvFilename` state; "Upload" then fires `CreateCodesUpload_ByPlaytestIdMutation({ playtestId, data: { csvContent, filename } })`. The `V1UploadCodesResponse.rejections[]` channel from PRD §4.3 surfaces as a red `<Alert>` listing `Line N: reason — value` (capped at 50 entries with a tail "+M more" line); on success the form clears. AGS_CAMPAIGN path renders an `<InputNumber>` (1–50,000) + "Generate more codes" + "Sync from AGS" buttons wired to `TopUp` / `SyncFromAg` mutations — the §9 hide-on-cut decision is unblocked since phase 9 shipped both. The codes table renders raw `value` + `state` tag + `createdAt` per the PRD §5.7 admin-exempt rule (codes are only redacted in logs, never in the admin UI). `LowPoolBanner` + the four-stat row (`Total/Unused/Reserved/Granted`) sit above both sections so operators see pool health regardless of distribution model.

    - **`scripts/smoke/admin-build.sh`** — sibling of `player-build.sh`. Runs `npm install` → reseeds `swaggers/playtesthub.json` from `gateway/apidocs/api.swagger.json` → `npm run cg:clean-and-generate` → `eslint` → `vitest` → `vite build`, then asserts `dist/remoteEntry.js` (Module Federation) + `dist/index.html` + at least one chunk under `dist/assets/*.js` are present. The codegen-from-local-swagger seed is what makes this script run independently of any deployed AGS namespace, matching the engineering.md §5 maintainer-only constraint until phase 12.1 rewires CI; CI itself is unchanged this phase (the existing `.github/workflows/ci.yml` admin-job-absent comment still applies because committing the generated client tree is a separate decision tracked by phase 12.1).

    **Tests** (8 new vitest cases on top of the 11 existing): renders applicant rows with status tag; approve mutation fires with `{ applicantId, data: {} }` after Popconfirm; Retry-DM button renders only for APPROVED+failed-DM rows (asserts exactly 1 button across a 3-row mixed-state fixture); low-pool banner appears when `unused/total ≤ 0.10`; reject modal accepts a typed reason and submits `{ rejectionReason }`; STEAM_KEYS code page renders the upload section + pool stats and hides the AGS section; AGS_CAMPAIGN code page renders generate/sync controls and hides the upload section; top-up + sync mutations fire with the correct payloads. The previous "AGS_CAMPAIGN radio is disabled" assertion was rewritten to "offers both distribution models with STEAM_KEYS as default" since phase 8 made the radio active. Existing 11 tests unchanged.

    **Verification**: admin `npm run lint` 0 issues, `npm run test` 19/19 PASS, `npm run build` produces `dist/remoteEntry.js` + chunks (the 1.36MB main chunk warning is pre-existing — no chunk-splitting work was added since it is out of scope for the phase). Repo gates: `go test ./...` PASS (full module — no Go code touched, but ran for completeness per CLAUDE.md), `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS (no new probes — phase 10 introduces no server-visible RPCs; the existing M2-handler-canary set already covers the wire surface this UI consumes), `scripts/smoke/admin-build.sh` PASS as the new frontend gate. The rejection-reason input was kept (not cut) — the Modal-with-TextArea is cheap enough that the cut tracker did not need to fire.
11. `[x]` **Player Svelte — NDA accept + code retrieval views** — extend `player/`: NDA accept route gates approve flow per PRD §4.1 step 4. Approved-state view renders code copy widget with model-specific instructions (STEAM_KEYS → "redeem on Steam" passthrough; AGS_CAMPAIGN → "redeem in-game via PublicRedeemCode"). `NdaReacceptRequired` blocks survey-submit (no-op until M3) but keeps `GRANTED` code visible per PRD §5.3. axe-core a11y assertions extended to the two new views (re-uses phase 12.1 scaffold once it lands; otherwise covered by the existing player-build smoke).

    **Outcome (2026-05-04)**: shipped. The NDA accept route already landed in M2 phase 4 (`player/src/routes/Nda.svelte`); this phase wires the approved-state code-retrieval surface into `Pending.svelte`. `player/src/lib/api.ts` adds `fetchGrantedCode(playtestId)` against `GET /v1/player/playtests/{id}/grantedCode` (proto §669–678) — typed return of `{value, distributionModel}`, bearer auth, 401/404 surfaced as `ApiError`. `player/src/routes/Pending.svelte` rewritten: on `APPLICANT_STATUS_APPROVED` it now fetches the granted code and renders a readonly text input + Copy button (clipboard write with a 2s "Copied" affordance and an `onfocus={select}` fallback for insecure contexts where `navigator.clipboard` is unavailable), followed by a model-specific redeem block — STEAM_KEYS shows the literal Steam client path ("Games → Activate a Product on Steam…"), AGS_CAMPAIGN shows the `PublicRedeemCode` in-game instruction. **PRD §5.3 §4.1 step 7 fix**: the previous "any hash mismatch → bounce to NDA route" branch unconditionally hid the granted code from already-approved players who had not yet re-accepted; rewritten to gate the bounce on `status==PENDING` only. APPROVED + re-accept-required now stays put, renders the code, and surfaces a yellow `role="status"` banner explaining the NDA was updated and re-accept is needed before any future survey submit (the survey itself remains an M3 no-op). Rejected/pending copy unchanged.

    **Tests** (8 vitest cases in `tests/Pending.test.ts`, replacing the prior 4 — covers the same redirects plus the new code surface): pending copy renders for `PENDING`; rejected copy renders for `REJECTED` (asserts no `rejectionReason` leak per PRD §5.4); 401 from playtest/applicant fetch redirects to landing; PENDING + hash mismatch bounces to NDA route; **APPROVED + STEAM_KEYS** renders the code value + "Activate a Product on Steam" instruction; **APPROVED + AGS_CAMPAIGN** renders the code value + `PublicRedeemCode` instruction; **APPROVED + re-accept required** keeps the code visible, shows the "NDA was updated" banner, and asserts the URL was *not* redirected to the NDA route (the regression this phase fixes); GetGrantedCode 503 surfaces a user-facing error via `role="alert"` without redirecting. New `tests/api.test.ts` cases cover the request shape (URL + bearer header + JSON response) and 404-bubbling for the soft-deleted-playtest path declared in `errors.md`.

    **Verification**: `(cd player && npm run test)` 68/68 PASS (was 53; +13 from this phase's expanded route coverage + 2 api.test cases — the prior STATUS phase 9 tally of 53 was stale, the actual baseline before this phase was 60 after phase 10's 7-case bump). `scripts/smoke/player-build.sh` PASS (vite build emits `dist/index.html` + JS chunks; `dist/config.json` ships only when the operator drops a real `public/config.json` in — the smoke harness explicitly rejects that case to keep operator config out of the bundle, which is how it should behave). Repo gates: `go test ./...` PASS (full module — no Go code touched), `golangci-lint run` 0 issues, `scripts/smoke/boot.sh` PASS (no new probes — phase 11 introduces no new server-visible RPC handlers; `GetGrantedCode` is already covered by the phase-6 canary). axe-core a11y assertions deferred to phase 12.1 per the phase 9 plan; the new code-retrieval surface uses semantic `<input>`/`<button>` + `role="status"`/`role="alert"` so the future axe pass should be a no-op.
12. `[x]` **`pth` CLI M2 subcommands + `pth flow golden-m2`** — all [`cli.md`](cli.md) §6.2 entries: `pth applicant accept-nda/list/approve/reject/retry-dm/get-code`, `pth code upload/top-up/sync-from-ags/pool`. Each subcommand registered in `cmd/pth/registry.go` with the same `slugFlag`/`idFlag` helper pattern as M1. `pth flow golden-m2` extends `golden-m1`: create → transition OPEN → signup → accept-nda → upload codes → approve → assert `status=APPROVED` + `GetGrantedCode` returns a non-empty value. NDJSON line shapes preserved (`flowSuccessLine`/`flowFailureLine`/`flowDryRunLine`). `pth describe` updated to include the new commands; golden-diff test refreshed.

    **Outcome (2026-05-04)**: shipped. `cmd/pth/applicant.go` extended with six M2 actions (`accept-nda`, `list`, `approve`, `reject`, `retry-dm`, `get-code`); new `cmd/pth/code.go` ships the four `code` actions; `cmd/pth/flow.go` adds `runFlowGoldenM2` orchestrating the seven-step PRD §4.1 M2 golden flow (create → transition-open → signup → accept-nda → upload-codes → approve → get-code) with the same NDJSON line shapes and stop-on-first-failure semantics as `golden-m1`. The orchestrator is split into `parseGoldenM2Flags` + four small step helpers (`flowGoldenM2CreateAndOpen`, `…SignupAndAccept`, `…UploadAndApprove`, `…GetCode`) so each function stays well below `gocognit`'s 30-cap. `--dry-run` emits seven `DRY_RUN` lines with `<resolved-after-create>` / `<resolved-after-signup>` placeholders for ids that only exist after a real dial. The CSV upload defaults to a synthesised `GOLDEN-M2-<SLUG>-NNNN` body so callers can run the flow without staging a CSV; `--codes-file` (path or `-` for stdin) overrides. `cmd/pth/registry.go` adds 11 catalogue entries (alphabetised) plus a `playtestFlag()` helper that mirrors `slugFlag()`/`idFlag()`. Stub plumbing extended in `cmd/pth/playtest_test.go` with the ten new RPC funcs; new `cmd/pth/code_test.go` (10 cases) and 10 new applicant cases cover dry-run, success, validation, filter propagation, and the optional `RejectionReason` shape. Three new `flow_test.go` cases cover the M2 happy path, stop-on-approve-failure, and dry-run/profile-required guards.

    **Tests**: `go test ./cmd/pth/` PASS — full module `go test ./...` PASS. `golangci-lint run` 0 issues across cmd + pkg + internal. `cmd/pth/testdata/describe.golden.json` regenerated (505 → 795 lines) — diff-checked by `TestRunDescribe_MatchesGoldenFile`.

    **Smoke**: `scripts/smoke/boot.sh` PASS unchanged (no new server-side handlers; M2 phase 9 already covered every M2 RPC). `scripts/smoke/pth.sh` extended with three new probes — (a) `flow golden-m2 --dry-run` asserts seven NDJSON lines in step order with `status=DRY_RUN`; (b) `pth describe` is grepped for every M2 §6.2 entry name (defence-in-depth alongside the byte-exact golden-file diff); (c) the existing `flow golden-m1 --dry-run` probe stays untouched. All probes PASS against the local stack.
13. `[x]` **E2E golden flow (M2 stop)** — extend `e2e/` with `TestGoldenM2Flow` driving the new CLI flow against the in-process backend (via `internal/bootapp` from M1 phase 11) + testcontainers-postgres. STEAM_KEYS path mandatory in CI. AGS_CAMPAIGN sub-test gated on the same env-var pattern as the existing `pth.sh` live block (`AGS_*` + `E2E_*`); skipped without creds. Teardown chain extends M1's: soft-delete the playtest, delete the test user, plus delete the AGS Item/Campaign on AGS_CAMPAIGN runs.

    **Outcome (2026-05-04)**: shipped. `e2e/golden_m2_test.go` mirrors M1's harness (same `suiteHarness` from `main_test.go`, same `runPTH`/`tryPTH`/`parseNDJSON`/`jsonNested` helpers — no harness duplication). The test boots the testcontainers-postgres + in-process bootapp + freshly built `pth` binary, then drives admin-login → user-create → login-as → `pth flow golden-m2 --slug e2e-m2-<unix>-<rand> --admin-profile … --player-profile …`. It asserts seven NDJSON lines in step order with `status=OK` (`create-playtest` → `transition-open` → `signup` → `accept-nda` → `upload-codes` → `approve` → `get-code`), then belt-and-braces verifies `get-code.response.value` is non-empty + matches the synthesised `GOLDEN-M2-…` STEAM-key prefix and `distribution_model=DISTRIBUTION_MODEL_STEAM_KEYS`. Cleanup chain extends M1's: `playtest delete --id <playtestId>` + `user delete --id <userId> --yes`, both via `t.Cleanup` so a failed assertion still tears down. AGS_CAMPAIGN sub-test deferred per phase 8.1 (no live AGS adapter wired yet — bootapp uses `ags.NewMemClient`); the env-var gate (`AGS_BASE_URL` / `AGS_NAMESPACE` / `AGS_IAM_CLIENT_ID` / `AGS_IAM_CLIENT_SECRET` / `E2E_USERNAME` / `E2E_PASSWORD`) keeps the suite green in CI without secrets — `go test -run TestGoldenM2Flow ./e2e/...` PASS in 0.05s with all six unset (skips cleanly), same shape as M1's gate.
14. `[x]` **CI + README walkthrough (M2 cadence)** — refresh README "Reproduce the golden flow" to use `pth flow golden-m2`; CI green on new e2e + smoke scripts; phase 12.1 player a11y gate (deferred from M1) covers the new player views if it has landed by then. STATUS.md M2 row flips to `shipped`.

    **Outcome (2026-05-04)**: shipped. README "Reproduce the golden flow" now drives `pth flow golden-m2` end-to-end — admin login + player provision unchanged from M1, but the composite call drops `golden-m1` and gains `--codes-count` flow context (synthesised STEAM key for the `upload-codes` step; `--codes-file <path>` for a real CSV). Expected-output block flipped from four NDJSON lines to seven (`accept-nda`, `upload-codes`, `approve`, `get-code` added in step order). Verification-checklist `pth flow golden-m1` line bumped to `golden-m2`. Both `e2e/golden_m1_test.go` and `e2e/golden_m2_test.go` are now linked from the same paragraph so operators see both gates. CI surface unchanged from M1 phase 12: `go test ./...` already covers `e2e/` (the new test skips when AGS creds absent, runs end-to-end when present), `golangci-lint`, `buf lint`, and `proto-fresh` jobs all green; no workflow edits required because `e2e/` was already in scope. M2 row at the top of this file flipped from `in progress` to `shipped`. Phase 12.1 player a11y gate stays deferred to M3 polish — out of M2 scope.

#### Cross-cutting rules

- **Smoke harness lands with the code that introduces it** (CLAUDE.md). Every phase 4–11 extends `scripts/smoke/` (and, once phase 12 lands, the corresponding `pth flow golden-m2` step) with coverage for the RPC paths it introduces.
- **Cut-if-behind tracker** (M2 section below) updated as phases 7, 9, and 10 land. Any cut decision recorded in this row's notes column above.
- **Mid-phase fixup phases** (cf. M1 phase 8.1, 9.1, 9.2) reserved as needed; not pre-planned.

### M3 — Survey + Discord notifications + polish/docs/demo

Detailed ordering TBD at M2 exit. Includes the DM circuit breaker (if not already in M2), `RetryFailedDms`, survey builder + response one-shot, audit-log viewer UI, perf proof point, demo video.

## How to update

When a milestone deliverable merges, flip its row to `in progress` or `shipped` as appropriate. Add notes inline in a third column if the status needs qualification (e.g. "shipped except responses CSV export — cut per §10 M3"). Do not use this file for PRD version history — that belongs in `CHANGELOG.md`.

### Cut-if-behind tracking

Features that may be cut from their target milestone if the team is behind schedule. Listed here (rather than in PRD §10) so the PRD stays aspirational while the build plan tracks scope risk. On cut, record the decision in the milestone row's notes column above.

#### M1
- **Markdown rendering** — superseded: PRD §5.1 now specifies plain-text rendering for `Playtest.description` in MVP. Markdown rendering is on the post-MVP backlog (PRD §10 Deferred). Not an active cut candidate for M1.

#### M2
- **AGS_CAMPAIGN top-up UI** — superseded: phase 9 shipped `TopUpCodes` + `SyncFromAGS` and phase 10 shipped both controls in the admin UI. Cut not exercised.
- **Rejection reasons** — superseded: phase 10 shipped the optional `rejectionReason` text field via the admin reject Modal. Cut not exercised.

#### M3
- **Responses CSV export** — ship responses viewer without export if behind. On PRD §10 Deferred backlog.
- **Drag-reorder for survey questions** — ship up/down buttons only if behind.
- **Webhook-channel fallback** — DM-only delivery if behind. On PRD §10 Deferred backlog as R2 mitigation.
- **Audit log viewer UI** (PRD §5.7 page 5) — ship without the viewer if behind. **Audit writes in M2 remain mandatory** regardless — only the read-side UI is cut.
