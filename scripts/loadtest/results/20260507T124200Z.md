# Loadtest report — loadtest-1778157687-9547

Generated: 2026-05-07T12:52:00Z
Driver: k6 `scripts/loadtest/signup.js` (constant-arrival-rate against `POST /v1/player/playtests/{slug}/signup`)
PRD §6 target: **500 signups / 10 min, p95 < 3s** (signup latency, backend portion).

## Run config

| Knob | Value |
| --- | --- |
| Target gateway | `https://abtestdewa-pong.internal.gamingservices.accelbyte.io/ext-abtestdewa-pong-playtesthub` |
| Playtest slug | `loadtest-1778157687-9547` |
| Token pool | 500 unique users |
| Duration | 10m |
| Arrival rate | 50/min |
| k6 exit | 0 |

## Results

| Metric | Value |
| --- | --- |
| HTTP requests | 501 |
| Error rate | 0.00% |
| Signup avg | 487ms |
| Signup p50 | 464ms |
| Signup p90 | 556ms |
| **Signup p95** | **606ms** |
| Signup p99 | 720ms |
| Signup max | 928ms |

## Verdict vs PRD §6

| Gate | Result |
| --- | --- |
| Signup p95 < 3000ms | ✅ PASS |
| Error rate < 1% | ✅ PASS |

## Insert-path verification (Neon)

Direct query against the Extend-managed Postgres after the run:

```sql
SELECT COUNT(*) AS rows, COUNT(DISTINCT user_id) AS distinct_users
FROM applicant
WHERE playtest_id = '5f1a2186-2d69-4e0a-b6f9-2ecd60d4bcf9';
```

| Field | Value |
| --- | --- |
| Rows | 500 |
| Distinct `user_id` | 500 |

501 HTTP requests − 500 inserts = 1 replay (final iteration wraps to
token 0; PRD §5.2 idempotent path). The reported p95 reflects the
**insert path** for 500 of 501 samples — not a degenerate replay-path
measurement. This check exists because the prior report
(`20260507T023635Z.md`) recorded p95 = 466 ms with only 11 inserts
behind it, and the rotation bug it surfaced is now fixed in
`signup.js` (uses `exec.scenario.iterationInTest`).

## Caveats applied

- **Discord OAuth excluded.** Test users are AGS-internal (no Discord
  ID). Backend Signup falls back to raw IAM `sub` per PRD §10 M1, so
  the Discord API round-trip is not measured. Browser → Discord OAuth →
  IAM token-exchange time is owned by Discord + AGS and must be
  measured separately to claim end-to-end p95.
- **Single replica.** PRD §6 baseline.
- **Discord DM delivery excluded.** `Signup` schedules no DM.
